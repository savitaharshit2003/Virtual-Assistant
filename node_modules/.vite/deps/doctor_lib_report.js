import {
  __commonJS,
  __require
} from "./chunk-7D4SUZUM.js";

// browser-external:fs
var require_fs = __commonJS({
  "browser-external:fs"(exports2, module2) {
    module2.exports = Object.create(new Proxy({}, {
      get(_, key) {
        if (key !== "__esModule" && key !== "__proto__" && key !== "constructor" && key !== "splice") {
          console.warn(`Module "fs" has been externalized for browser compatibility. Cannot access "fs.${key}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
        }
      }
    }));
  }
});

// browser-external:path
var require_path = __commonJS({
  "browser-external:path"(exports2, module2) {
    module2.exports = Object.create(new Proxy({}, {
      get(_, key) {
        if (key !== "__esModule" && key !== "__proto__" && key !== "constructor" && key !== "splice") {
          console.warn(`Module "path" has been externalized for browser compatibility. Cannot access "path.${key}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
        }
      }
    }));
  }
});

// ../../../../node_modules/async/lib/async.js
var require_async = __commonJS({
  "../../../../node_modules/async/lib/async.js"(exports2, module2) {
    (function() {
      var async = {};
      var root, previous_async;
      root = this;
      if (root != null) {
        previous_async = root.async;
      }
      async.noConflict = function() {
        root.async = previous_async;
        return async;
      };
      function only_once(fn) {
        var called = false;
        return function() {
          if (called) throw new Error("Callback was already called.");
          called = true;
          fn.apply(root, arguments);
        };
      }
      var _each = function(arr, iterator) {
        if (arr.forEach) {
          return arr.forEach(iterator);
        }
        for (var i = 0; i < arr.length; i += 1) {
          iterator(arr[i], i, arr);
        }
      };
      var _map = function(arr, iterator) {
        if (arr.map) {
          return arr.map(iterator);
        }
        var results = [];
        _each(arr, function(x, i, a) {
          results.push(iterator(x, i, a));
        });
        return results;
      };
      var _reduce = function(arr, iterator, memo) {
        if (arr.reduce) {
          return arr.reduce(iterator, memo);
        }
        _each(arr, function(x, i, a) {
          memo = iterator(memo, x, i, a);
        });
        return memo;
      };
      var _keys = function(obj) {
        if (Object.keys) {
          return Object.keys(obj);
        }
        var keys2 = [];
        for (var k in obj) {
          if (obj.hasOwnProperty(k)) {
            keys2.push(k);
          }
        }
        return keys2;
      };
      if (typeof process === "undefined" || !process.nextTick) {
        if (typeof setImmediate === "function") {
          async.nextTick = function(fn) {
            setImmediate(fn);
          };
          async.setImmediate = async.nextTick;
        } else {
          async.nextTick = function(fn) {
            setTimeout(fn, 0);
          };
          async.setImmediate = async.nextTick;
        }
      } else {
        async.nextTick = process.nextTick;
        if (typeof setImmediate !== "undefined") {
          async.setImmediate = function(fn) {
            setImmediate(fn);
          };
        } else {
          async.setImmediate = async.nextTick;
        }
      }
      async.each = function(arr, iterator, callback) {
        callback = callback || function() {
        };
        if (!arr.length) {
          return callback();
        }
        var completed = 0;
        _each(arr, function(x) {
          iterator(x, only_once(function(err) {
            if (err) {
              callback(err);
              callback = function() {
              };
            } else {
              completed += 1;
              if (completed >= arr.length) {
                callback(null);
              }
            }
          }));
        });
      };
      async.forEach = async.each;
      async.eachSeries = function(arr, iterator, callback) {
        callback = callback || function() {
        };
        if (!arr.length) {
          return callback();
        }
        var completed = 0;
        var iterate = function() {
          iterator(arr[completed], function(err) {
            if (err) {
              callback(err);
              callback = function() {
              };
            } else {
              completed += 1;
              if (completed >= arr.length) {
                callback(null);
              } else {
                iterate();
              }
            }
          });
        };
        iterate();
      };
      async.forEachSeries = async.eachSeries;
      async.eachLimit = function(arr, limit, iterator, callback) {
        var fn = _eachLimit(limit);
        fn.apply(null, [arr, iterator, callback]);
      };
      async.forEachLimit = async.eachLimit;
      var _eachLimit = function(limit) {
        return function(arr, iterator, callback) {
          callback = callback || function() {
          };
          if (!arr.length || limit <= 0) {
            return callback();
          }
          var completed = 0;
          var started = 0;
          var running = 0;
          (function replenish() {
            if (completed >= arr.length) {
              return callback();
            }
            while (running < limit && started < arr.length) {
              started += 1;
              running += 1;
              iterator(arr[started - 1], function(err) {
                if (err) {
                  callback(err);
                  callback = function() {
                  };
                } else {
                  completed += 1;
                  running -= 1;
                  if (completed >= arr.length) {
                    callback();
                  } else {
                    replenish();
                  }
                }
              });
            }
          })();
        };
      };
      var doParallel = function(fn) {
        return function() {
          var args = Array.prototype.slice.call(arguments);
          return fn.apply(null, [async.each].concat(args));
        };
      };
      var doParallelLimit = function(limit, fn) {
        return function() {
          var args = Array.prototype.slice.call(arguments);
          return fn.apply(null, [_eachLimit(limit)].concat(args));
        };
      };
      var doSeries = function(fn) {
        return function() {
          var args = Array.prototype.slice.call(arguments);
          return fn.apply(null, [async.eachSeries].concat(args));
        };
      };
      var _asyncMap = function(eachfn, arr, iterator, callback) {
        var results = [];
        arr = _map(arr, function(x, i) {
          return { index: i, value: x };
        });
        eachfn(arr, function(x, callback2) {
          iterator(x.value, function(err, v) {
            results[x.index] = v;
            callback2(err);
          });
        }, function(err) {
          callback(err, results);
        });
      };
      async.map = doParallel(_asyncMap);
      async.mapSeries = doSeries(_asyncMap);
      async.mapLimit = function(arr, limit, iterator, callback) {
        return _mapLimit(limit)(arr, iterator, callback);
      };
      var _mapLimit = function(limit) {
        return doParallelLimit(limit, _asyncMap);
      };
      async.reduce = function(arr, memo, iterator, callback) {
        async.eachSeries(arr, function(x, callback2) {
          iterator(memo, x, function(err, v) {
            memo = v;
            callback2(err);
          });
        }, function(err) {
          callback(err, memo);
        });
      };
      async.inject = async.reduce;
      async.foldl = async.reduce;
      async.reduceRight = function(arr, memo, iterator, callback) {
        var reversed = _map(arr, function(x) {
          return x;
        }).reverse();
        async.reduce(reversed, memo, iterator, callback);
      };
      async.foldr = async.reduceRight;
      var _filter = function(eachfn, arr, iterator, callback) {
        var results = [];
        arr = _map(arr, function(x, i) {
          return { index: i, value: x };
        });
        eachfn(arr, function(x, callback2) {
          iterator(x.value, function(v) {
            if (v) {
              results.push(x);
            }
            callback2();
          });
        }, function(err) {
          callback(_map(results.sort(function(a, b) {
            return a.index - b.index;
          }), function(x) {
            return x.value;
          }));
        });
      };
      async.filter = doParallel(_filter);
      async.filterSeries = doSeries(_filter);
      async.select = async.filter;
      async.selectSeries = async.filterSeries;
      var _reject = function(eachfn, arr, iterator, callback) {
        var results = [];
        arr = _map(arr, function(x, i) {
          return { index: i, value: x };
        });
        eachfn(arr, function(x, callback2) {
          iterator(x.value, function(v) {
            if (!v) {
              results.push(x);
            }
            callback2();
          });
        }, function(err) {
          callback(_map(results.sort(function(a, b) {
            return a.index - b.index;
          }), function(x) {
            return x.value;
          }));
        });
      };
      async.reject = doParallel(_reject);
      async.rejectSeries = doSeries(_reject);
      var _detect = function(eachfn, arr, iterator, main_callback) {
        eachfn(arr, function(x, callback) {
          iterator(x, function(result2) {
            if (result2) {
              main_callback(x);
              main_callback = function() {
              };
            } else {
              callback();
            }
          });
        }, function(err) {
          main_callback();
        });
      };
      async.detect = doParallel(_detect);
      async.detectSeries = doSeries(_detect);
      async.some = function(arr, iterator, main_callback) {
        async.each(arr, function(x, callback) {
          iterator(x, function(v) {
            if (v) {
              main_callback(true);
              main_callback = function() {
              };
            }
            callback();
          });
        }, function(err) {
          main_callback(false);
        });
      };
      async.any = async.some;
      async.every = function(arr, iterator, main_callback) {
        async.each(arr, function(x, callback) {
          iterator(x, function(v) {
            if (!v) {
              main_callback(false);
              main_callback = function() {
              };
            }
            callback();
          });
        }, function(err) {
          main_callback(true);
        });
      };
      async.all = async.every;
      async.sortBy = function(arr, iterator, callback) {
        async.map(arr, function(x, callback2) {
          iterator(x, function(err, criteria) {
            if (err) {
              callback2(err);
            } else {
              callback2(null, { value: x, criteria });
            }
          });
        }, function(err, results) {
          if (err) {
            return callback(err);
          } else {
            var fn = function(left, right) {
              var a = left.criteria, b = right.criteria;
              return a < b ? -1 : a > b ? 1 : 0;
            };
            callback(null, _map(results.sort(fn), function(x) {
              return x.value;
            }));
          }
        });
      };
      async.auto = function(tasks, callback) {
        callback = callback || function() {
        };
        var keys2 = _keys(tasks);
        if (!keys2.length) {
          return callback(null);
        }
        var results = {};
        var listeners = [];
        var addListener = function(fn) {
          listeners.unshift(fn);
        };
        var removeListener = function(fn) {
          for (var i = 0; i < listeners.length; i += 1) {
            if (listeners[i] === fn) {
              listeners.splice(i, 1);
              return;
            }
          }
        };
        var taskComplete = function() {
          _each(listeners.slice(0), function(fn) {
            fn();
          });
        };
        addListener(function() {
          if (_keys(results).length === keys2.length) {
            callback(null, results);
            callback = function() {
            };
          }
        });
        _each(keys2, function(k) {
          var task = tasks[k] instanceof Function ? [tasks[k]] : tasks[k];
          var taskCallback = function(err) {
            var args = Array.prototype.slice.call(arguments, 1);
            if (args.length <= 1) {
              args = args[0];
            }
            if (err) {
              var safeResults = {};
              _each(_keys(results), function(rkey) {
                safeResults[rkey] = results[rkey];
              });
              safeResults[k] = args;
              callback(err, safeResults);
              callback = function() {
              };
            } else {
              results[k] = args;
              async.setImmediate(taskComplete);
            }
          };
          var requires = task.slice(0, Math.abs(task.length - 1)) || [];
          var ready = function() {
            return _reduce(requires, function(a, x) {
              return a && results.hasOwnProperty(x);
            }, true) && !results.hasOwnProperty(k);
          };
          if (ready()) {
            task[task.length - 1](taskCallback, results);
          } else {
            var listener = function() {
              if (ready()) {
                removeListener(listener);
                task[task.length - 1](taskCallback, results);
              }
            };
            addListener(listener);
          }
        });
      };
      async.waterfall = function(tasks, callback) {
        callback = callback || function() {
        };
        if (tasks.constructor !== Array) {
          var err = new Error("First argument to waterfall must be an array of functions");
          return callback(err);
        }
        if (!tasks.length) {
          return callback();
        }
        var wrapIterator = function(iterator) {
          return function(err2) {
            if (err2) {
              callback.apply(null, arguments);
              callback = function() {
              };
            } else {
              var args = Array.prototype.slice.call(arguments, 1);
              var next = iterator.next();
              if (next) {
                args.push(wrapIterator(next));
              } else {
                args.push(callback);
              }
              async.setImmediate(function() {
                iterator.apply(null, args);
              });
            }
          };
        };
        wrapIterator(async.iterator(tasks))();
      };
      var _parallel = function(eachfn, tasks, callback) {
        callback = callback || function() {
        };
        if (tasks.constructor === Array) {
          eachfn.map(tasks, function(fn, callback2) {
            if (fn) {
              fn(function(err) {
                var args = Array.prototype.slice.call(arguments, 1);
                if (args.length <= 1) {
                  args = args[0];
                }
                callback2.call(null, err, args);
              });
            }
          }, callback);
        } else {
          var results = {};
          eachfn.each(_keys(tasks), function(k, callback2) {
            tasks[k](function(err) {
              var args = Array.prototype.slice.call(arguments, 1);
              if (args.length <= 1) {
                args = args[0];
              }
              results[k] = args;
              callback2(err);
            });
          }, function(err) {
            callback(err, results);
          });
        }
      };
      async.parallel = function(tasks, callback) {
        _parallel({ map: async.map, each: async.each }, tasks, callback);
      };
      async.parallelLimit = function(tasks, limit, callback) {
        _parallel({ map: _mapLimit(limit), each: _eachLimit(limit) }, tasks, callback);
      };
      async.series = function(tasks, callback) {
        callback = callback || function() {
        };
        if (tasks.constructor === Array) {
          async.mapSeries(tasks, function(fn, callback2) {
            if (fn) {
              fn(function(err) {
                var args = Array.prototype.slice.call(arguments, 1);
                if (args.length <= 1) {
                  args = args[0];
                }
                callback2.call(null, err, args);
              });
            }
          }, callback);
        } else {
          var results = {};
          async.eachSeries(_keys(tasks), function(k, callback2) {
            tasks[k](function(err) {
              var args = Array.prototype.slice.call(arguments, 1);
              if (args.length <= 1) {
                args = args[0];
              }
              results[k] = args;
              callback2(err);
            });
          }, function(err) {
            callback(err, results);
          });
        }
      };
      async.iterator = function(tasks) {
        var makeCallback = function(index) {
          var fn = function() {
            if (tasks.length) {
              tasks[index].apply(null, arguments);
            }
            return fn.next();
          };
          fn.next = function() {
            return index < tasks.length - 1 ? makeCallback(index + 1) : null;
          };
          return fn;
        };
        return makeCallback(0);
      };
      async.apply = function(fn) {
        var args = Array.prototype.slice.call(arguments, 1);
        return function() {
          return fn.apply(
            null,
            args.concat(Array.prototype.slice.call(arguments))
          );
        };
      };
      var _concat = function(eachfn, arr, fn, callback) {
        var r = [];
        eachfn(arr, function(x, cb) {
          fn(x, function(err, y) {
            r = r.concat(y || []);
            cb(err);
          });
        }, function(err) {
          callback(err, r);
        });
      };
      async.concat = doParallel(_concat);
      async.concatSeries = doSeries(_concat);
      async.whilst = function(test, iterator, callback) {
        if (test()) {
          iterator(function(err) {
            if (err) {
              return callback(err);
            }
            async.whilst(test, iterator, callback);
          });
        } else {
          callback();
        }
      };
      async.doWhilst = function(iterator, test, callback) {
        iterator(function(err) {
          if (err) {
            return callback(err);
          }
          if (test()) {
            async.doWhilst(iterator, test, callback);
          } else {
            callback();
          }
        });
      };
      async.until = function(test, iterator, callback) {
        if (!test()) {
          iterator(function(err) {
            if (err) {
              return callback(err);
            }
            async.until(test, iterator, callback);
          });
        } else {
          callback();
        }
      };
      async.doUntil = function(iterator, test, callback) {
        iterator(function(err) {
          if (err) {
            return callback(err);
          }
          if (!test()) {
            async.doUntil(iterator, test, callback);
          } else {
            callback();
          }
        });
      };
      async.queue = function(worker, concurrency) {
        if (concurrency === void 0) {
          concurrency = 1;
        }
        function _insert(q2, data, pos, callback) {
          if (data.constructor !== Array) {
            data = [data];
          }
          _each(data, function(task) {
            var item = {
              data: task,
              callback: typeof callback === "function" ? callback : null
            };
            if (pos) {
              q2.tasks.unshift(item);
            } else {
              q2.tasks.push(item);
            }
            if (q2.saturated && q2.tasks.length === concurrency) {
              q2.saturated();
            }
            async.setImmediate(q2.process);
          });
        }
        var workers = 0;
        var q = {
          tasks: [],
          concurrency,
          saturated: null,
          empty: null,
          drain: null,
          push: function(data, callback) {
            _insert(q, data, false, callback);
          },
          unshift: function(data, callback) {
            _insert(q, data, true, callback);
          },
          process: function() {
            if (workers < q.concurrency && q.tasks.length) {
              var task = q.tasks.shift();
              if (q.empty && q.tasks.length === 0) {
                q.empty();
              }
              workers += 1;
              var next = function() {
                workers -= 1;
                if (task.callback) {
                  task.callback.apply(task, arguments);
                }
                if (q.drain && q.tasks.length + workers === 0) {
                  q.drain();
                }
                q.process();
              };
              var cb = only_once(next);
              worker(task.data, cb);
            }
          },
          length: function() {
            return q.tasks.length;
          },
          running: function() {
            return workers;
          }
        };
        return q;
      };
      async.cargo = function(worker, payload) {
        var working = false, tasks = [];
        var cargo = {
          tasks,
          payload,
          saturated: null,
          empty: null,
          drain: null,
          push: function(data, callback) {
            if (data.constructor !== Array) {
              data = [data];
            }
            _each(data, function(task) {
              tasks.push({
                data: task,
                callback: typeof callback === "function" ? callback : null
              });
              if (cargo.saturated && tasks.length === payload) {
                cargo.saturated();
              }
            });
            async.setImmediate(cargo.process);
          },
          process: function process2() {
            if (working) return;
            if (tasks.length === 0) {
              if (cargo.drain) cargo.drain();
              return;
            }
            var ts = typeof payload === "number" ? tasks.splice(0, payload) : tasks.splice(0);
            var ds = _map(ts, function(task) {
              return task.data;
            });
            if (cargo.empty) cargo.empty();
            working = true;
            worker(ds, function() {
              working = false;
              var args = arguments;
              _each(ts, function(data) {
                if (data.callback) {
                  data.callback.apply(null, args);
                }
              });
              process2();
            });
          },
          length: function() {
            return tasks.length;
          },
          running: function() {
            return working;
          }
        };
        return cargo;
      };
      var _console_fn = function(name) {
        return function(fn) {
          var args = Array.prototype.slice.call(arguments, 1);
          fn.apply(null, args.concat([function(err) {
            var args2 = Array.prototype.slice.call(arguments, 1);
            if (typeof console !== "undefined") {
              if (err) {
                if (console.error) {
                  console.error(err);
                }
              } else if (console[name]) {
                _each(args2, function(x) {
                  console[name](x);
                });
              }
            }
          }]));
        };
      };
      async.log = _console_fn("log");
      async.dir = _console_fn("dir");
      async.memoize = function(fn, hasher) {
        var memo = {};
        var queues = {};
        hasher = hasher || function(x) {
          return x;
        };
        var memoized = function() {
          var args = Array.prototype.slice.call(arguments);
          var callback = args.pop();
          var key = hasher.apply(null, args);
          if (key in memo) {
            callback.apply(null, memo[key]);
          } else if (key in queues) {
            queues[key].push(callback);
          } else {
            queues[key] = [callback];
            fn.apply(null, args.concat([function() {
              memo[key] = arguments;
              var q = queues[key];
              delete queues[key];
              for (var i = 0, l = q.length; i < l; i++) {
                q[i].apply(null, arguments);
              }
            }]));
          }
        };
        memoized.memo = memo;
        memoized.unmemoized = fn;
        return memoized;
      };
      async.unmemoize = function(fn) {
        return function() {
          return (fn.unmemoized || fn).apply(null, arguments);
        };
      };
      async.times = function(count, iterator, callback) {
        var counter = [];
        for (var i = 0; i < count; i++) {
          counter.push(i);
        }
        return async.map(counter, iterator, callback);
      };
      async.timesSeries = function(count, iterator, callback) {
        var counter = [];
        for (var i = 0; i < count; i++) {
          counter.push(i);
        }
        return async.mapSeries(counter, iterator, callback);
      };
      async.compose = function() {
        var fns = Array.prototype.reverse.call(arguments);
        return function() {
          var that2 = this;
          var args = Array.prototype.slice.call(arguments);
          var callback = args.pop();
          async.reduce(
            fns,
            args,
            function(newargs, fn, cb) {
              fn.apply(that2, newargs.concat([function() {
                var err = arguments[0];
                var nextargs = Array.prototype.slice.call(arguments, 1);
                cb(err, nextargs);
              }]));
            },
            function(err, results) {
              callback.apply(that2, [err].concat(results));
            }
          );
        };
      };
      var _applyEach = function(eachfn, fns) {
        var go = function() {
          var that2 = this;
          var args2 = Array.prototype.slice.call(arguments);
          var callback = args2.pop();
          return eachfn(
            fns,
            function(fn, cb) {
              fn.apply(that2, args2.concat([cb]));
            },
            callback
          );
        };
        if (arguments.length > 2) {
          var args = Array.prototype.slice.call(arguments, 2);
          return go.apply(this, args);
        } else {
          return go;
        }
      };
      async.applyEach = doParallel(_applyEach);
      async.applyEachSeries = doSeries(_applyEach);
      async.forever = function(fn, callback) {
        function next(err) {
          if (err) {
            if (callback) {
              return callback(err);
            }
            throw err;
          }
          fn(next);
        }
        next();
      };
      if (typeof define !== "undefined" && define.amd) {
        define([], function() {
          return async;
        });
      } else if (typeof module2 !== "undefined" && module2.exports) {
        module2.exports = async;
      } else {
        root.async = async;
      }
    })();
  }
});

// ../../../../node_modules/fs-compare/index.js
var require_fs_compare = __commonJS({
  "../../../../node_modules/fs-compare/index.js"(exports2, module2) {
    var fs = require_fs();
    var stat = function(key, fileName, cb) {
      fs.stat(fileName, function(err, stat2) {
        if (err) {
          return cb(err);
        }
        cb(null, stat2[key]);
      });
    };
    var statSync = function(key, fileName) {
      var stat2 = fs.statSync(fileName);
      return stat2[key];
    };
    var compareTest = function(errA, valueA, errB, valueB) {
      if (errA && errA.code !== "ENOENT") {
        return [errA];
      }
      if (errB && errB.code !== "ENOENT") {
        return [errB];
      }
      if (valueA instanceof Date) {
        valueA = valueA.getTime();
      }
      if (valueB instanceof Date) {
        valueB = valueB.getTime();
      }
      if (errA && errB) {
        return [null, 0];
      }
      if (errA || valueA < valueB) {
        return [null, -1];
      }
      if (errB || valueA > valueB) {
        return [null, 1];
      }
      return [null, 0];
    };
    var fsCompare = function(statFn, fileNameA, fileNameB, cb) {
      statFn(fileNameA, function(errA, valueA) {
        statFn(fileNameB, function(errB, valueB) {
          var result2 = compareTest(errA, valueA, errB, valueB);
          return cb(result2[0], result2[1]);
        });
      });
    };
    var fsCompareSync = function(statFn, fileNameA, fileNameB) {
      var valueA;
      var valueB;
      var errA;
      var errB;
      try {
        valueA = statFn(fileNameA);
      } catch (err) {
        errA = err;
      }
      try {
        valueB = statFn(fileNameB);
      } catch (err) {
        errB = err;
      }
      var result2 = compareTest(errA, valueA, errB, valueB);
      if (result2[0]) {
        throw result2[0];
      } else {
        return result2[1];
      }
    };
    ["size", "atime", "mtime", "ctime"].forEach(function(key) {
      fsCompare[key] = fsCompare.bind(null, stat.bind(null, key));
      fsCompareSync[key] = fsCompareSync.bind(null, statSync.bind(null, key));
    });
    fsCompare.sync = fsCompareSync;
    module2.exports = fsCompare;
  }
});

// ../../../../node_modules/pegjs/lib/peg.js
var require_peg = __commonJS({
  "../../../../node_modules/pegjs/lib/peg.js"(exports, module) {
    var PEG = (function(undefined) {
      var PEG = {
        /* PEG.js version (uses semantic versioning). */
        VERSION: "0.7.0",
        /*
         * Generates a parser from a specified grammar and returns it.
         *
         * The grammar must be a string in the format described by the metagramar in
         * the parser.pegjs file.
         *
         * Throws |PEG.parser.SyntaxError| if the grammar contains a syntax error or
         * |PEG.GrammarError| if it contains a semantic error. Note that not all
         * errors are detected during the generation and some may protrude to the
         * generated parser and cause its malfunction.
         */
        buildParser: function(grammar, options2) {
          return PEG.compiler.compile(PEG.parser.parse(grammar), options2);
        }
      };
      PEG.GrammarError = function(message) {
        this.name = "PEG.GrammarError";
        this.message = message;
      };
      PEG.GrammarError.prototype = Error.prototype;
      function range(start, stop) {
        if (stop === undefined) {
          stop = start;
          start = 0;
        }
        var result2 = new Array(Math.max(0, stop - start));
        for (var i = 0, j = start; j < stop; i++, j++) {
          result2[i] = j;
        }
        return result2;
      }
      function find(array, callback) {
        var length = array.length;
        for (var i = 0; i < length; i++) {
          if (callback(array[i])) {
            return array[i];
          }
        }
      }
      function contains(array, value) {
        var length = array.length;
        for (var i = 0; i < length; i++) {
          if (array[i] === value) {
            return true;
          }
        }
        return false;
      }
      function each(array, callback) {
        var length = array.length;
        for (var i = 0; i < length; i++) {
          callback(array[i], i);
        }
      }
      function map(array, callback) {
        var result2 = [];
        var length = array.length;
        for (var i = 0; i < length; i++) {
          result2[i] = callback(array[i], i);
        }
        return result2;
      }
      function pluck(array, key) {
        return map(array, function(e) {
          return e[key];
        });
      }
      function keys(object) {
        var result2 = [];
        for (var key in object) {
          result2.push(key);
        }
        return result2;
      }
      function values(object) {
        var result2 = [];
        for (var key in object) {
          result2.push(object[key]);
        }
        return result2;
      }
      function padLeft(input, padding, length) {
        var result2 = input;
        var padLength = length - input.length;
        for (var i = 0; i < padLength; i++) {
          result2 = padding + result2;
        }
        return result2;
      }
      function escape(ch) {
        var charCode = ch.charCodeAt(0);
        var escapeChar;
        var length;
        if (charCode <= 255) {
          escapeChar = "x";
          length = 2;
        } else {
          escapeChar = "u";
          length = 4;
        }
        return "\\" + escapeChar + padLeft(charCode.toString(16).toUpperCase(), "0", length);
      }
      function quote(s) {
        return '"' + s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\x08/g, "\\b").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\f/g, "\\f").replace(/\r/g, "\\r").replace(/[\x00-\x07\x0B\x0E-\x1F\x80-\uFFFF]/g, escape) + '"';
      }
      function quoteForRegexpClass(s) {
        return s.replace(/\\/g, "\\\\").replace(/\//g, "\\/").replace(/\]/g, "\\]").replace(/-/g, "\\-").replace(/\0/g, "\\0").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\v/g, "\\x0B").replace(/\f/g, "\\f").replace(/\r/g, "\\r").replace(/[\x01-\x08\x0E-\x1F\x80-\uFFFF]/g, escape);
      }
      function buildNodeVisitor(functions) {
        return function(node) {
          return functions[node.type].apply(null, arguments);
        };
      }
      function findRuleByName(ast2, name) {
        return find(ast2.rules, function(r) {
          return r.name === name;
        });
      }
      PEG.parser = (function() {
        function quote2(s) {
          return '"' + s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\x08/g, "\\b").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\f/g, "\\f").replace(/\r/g, "\\r").replace(/[\x00-\x07\x0B\x0E-\x1F\x80-\uFFFF]/g, escape) + '"';
        }
        var result2 = {
          /*
           * Parses the input with a generated parser. If the parsing is successfull,
           * returns a value explicitly or implicitly specified by the grammar from
           * which the parser was generated (see |PEG.buildParser|). If the parsing is
           * unsuccessful, throws |PEG.parser.SyntaxError| describing the error.
           */
          parse: function(input, startRule) {
            var parseFunctions = {
              "grammar": parse_grammar,
              "initializer": parse_initializer,
              "rule": parse_rule,
              "choice": parse_choice,
              "sequence": parse_sequence,
              "labeled": parse_labeled,
              "prefixed": parse_prefixed,
              "suffixed": parse_suffixed,
              "primary": parse_primary,
              "action": parse_action,
              "braced": parse_braced,
              "nonBraceCharacters": parse_nonBraceCharacters,
              "nonBraceCharacter": parse_nonBraceCharacter,
              "equals": parse_equals,
              "colon": parse_colon,
              "semicolon": parse_semicolon,
              "slash": parse_slash,
              "and": parse_and,
              "not": parse_not,
              "question": parse_question,
              "star": parse_star,
              "plus": parse_plus,
              "lparen": parse_lparen,
              "rparen": parse_rparen,
              "dot": parse_dot,
              "identifier": parse_identifier,
              "literal": parse_literal,
              "string": parse_string,
              "doubleQuotedString": parse_doubleQuotedString,
              "doubleQuotedCharacter": parse_doubleQuotedCharacter,
              "simpleDoubleQuotedCharacter": parse_simpleDoubleQuotedCharacter,
              "singleQuotedString": parse_singleQuotedString,
              "singleQuotedCharacter": parse_singleQuotedCharacter,
              "simpleSingleQuotedCharacter": parse_simpleSingleQuotedCharacter,
              "class": parse_class,
              "classCharacterRange": parse_classCharacterRange,
              "classCharacter": parse_classCharacter,
              "bracketDelimitedCharacter": parse_bracketDelimitedCharacter,
              "simpleBracketDelimitedCharacter": parse_simpleBracketDelimitedCharacter,
              "simpleEscapeSequence": parse_simpleEscapeSequence,
              "zeroEscapeSequence": parse_zeroEscapeSequence,
              "hexEscapeSequence": parse_hexEscapeSequence,
              "unicodeEscapeSequence": parse_unicodeEscapeSequence,
              "eolEscapeSequence": parse_eolEscapeSequence,
              "digit": parse_digit,
              "hexDigit": parse_hexDigit,
              "letter": parse_letter,
              "lowerCaseLetter": parse_lowerCaseLetter,
              "upperCaseLetter": parse_upperCaseLetter,
              "__": parse___,
              "comment": parse_comment,
              "singleLineComment": parse_singleLineComment,
              "multiLineComment": parse_multiLineComment,
              "eol": parse_eol,
              "eolChar": parse_eolChar,
              "whitespace": parse_whitespace
            };
            if (startRule !== undefined) {
              if (parseFunctions[startRule] === undefined) {
                throw new Error("Invalid rule name: " + quote2(startRule) + ".");
              }
            } else {
              startRule = "grammar";
            }
            var pos = 0;
            var reportFailures = 0;
            var rightmostFailuresPos = 0;
            var rightmostFailuresExpected = [];
            function padLeft2(input2, padding, length) {
              var result4 = input2;
              var padLength = length - input2.length;
              for (var i = 0; i < padLength; i++) {
                result4 = padding + result4;
              }
              return result4;
            }
            function escape2(ch) {
              var charCode = ch.charCodeAt(0);
              var escapeChar;
              var length;
              if (charCode <= 255) {
                escapeChar = "x";
                length = 2;
              } else {
                escapeChar = "u";
                length = 4;
              }
              return "\\" + escapeChar + padLeft2(charCode.toString(16).toUpperCase(), "0", length);
            }
            function matchFailed(failure) {
              if (pos < rightmostFailuresPos) {
                return;
              }
              if (pos > rightmostFailuresPos) {
                rightmostFailuresPos = pos;
                rightmostFailuresExpected = [];
              }
              rightmostFailuresExpected.push(failure);
            }
            function parse_grammar() {
              var result0, result1, result22, result32;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              result0 = parse___();
              if (result0 !== null) {
                result1 = parse_initializer();
                result1 = result1 !== null ? result1 : "";
                if (result1 !== null) {
                  result32 = parse_rule();
                  if (result32 !== null) {
                    result22 = [];
                    while (result32 !== null) {
                      result22.push(result32);
                      result32 = parse_rule();
                    }
                  } else {
                    result22 = null;
                  }
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, initializer, rules) {
                  return {
                    type: "grammar",
                    initializer: initializer !== "" ? initializer : null,
                    rules,
                    startRule: rules[0].name
                  };
                })(pos0, result0[1], result0[2]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_initializer() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_action();
              if (result0 !== null) {
                result1 = parse_semicolon();
                result1 = result1 !== null ? result1 : "";
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, code) {
                  return {
                    type: "initializer",
                    code
                  };
                })(pos0, result0[0]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_rule() {
              var result0, result1, result22, result32, result4;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_identifier();
              if (result0 !== null) {
                result1 = parse_string();
                result1 = result1 !== null ? result1 : "";
                if (result1 !== null) {
                  result22 = parse_equals();
                  if (result22 !== null) {
                    result32 = parse_choice();
                    if (result32 !== null) {
                      result4 = parse_semicolon();
                      result4 = result4 !== null ? result4 : "";
                      if (result4 !== null) {
                        result0 = [result0, result1, result22, result32, result4];
                      } else {
                        result0 = null;
                        pos = pos1;
                      }
                    } else {
                      result0 = null;
                      pos = pos1;
                    }
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, name, displayName, expression) {
                  return {
                    type: "rule",
                    name,
                    displayName: displayName !== "" ? displayName : null,
                    expression
                  };
                })(pos0, result0[0], result0[1], result0[3]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_choice() {
              var result0, result1, result22, result32;
              var pos0, pos1, pos2;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_sequence();
              if (result0 !== null) {
                result1 = [];
                pos2 = pos;
                result22 = parse_slash();
                if (result22 !== null) {
                  result32 = parse_sequence();
                  if (result32 !== null) {
                    result22 = [result22, result32];
                  } else {
                    result22 = null;
                    pos = pos2;
                  }
                } else {
                  result22 = null;
                  pos = pos2;
                }
                while (result22 !== null) {
                  result1.push(result22);
                  pos2 = pos;
                  result22 = parse_slash();
                  if (result22 !== null) {
                    result32 = parse_sequence();
                    if (result32 !== null) {
                      result22 = [result22, result32];
                    } else {
                      result22 = null;
                      pos = pos2;
                    }
                  } else {
                    result22 = null;
                    pos = pos2;
                  }
                }
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, head, tail) {
                  if (tail.length > 0) {
                    var alternatives = [head].concat(map(
                      tail,
                      function(element) {
                        return element[1];
                      }
                    ));
                    return {
                      type: "choice",
                      alternatives
                    };
                  } else {
                    return head;
                  }
                })(pos0, result0[0], result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_sequence() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              result0 = [];
              result1 = parse_labeled();
              while (result1 !== null) {
                result0.push(result1);
                result1 = parse_labeled();
              }
              if (result0 !== null) {
                result1 = parse_action();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, elements, code) {
                  var expression = elements.length !== 1 ? {
                    type: "sequence",
                    elements
                  } : elements[0];
                  return {
                    type: "action",
                    expression,
                    code
                  };
                })(pos0, result0[0], result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              if (result0 === null) {
                pos0 = pos;
                result0 = [];
                result1 = parse_labeled();
                while (result1 !== null) {
                  result0.push(result1);
                  result1 = parse_labeled();
                }
                if (result0 !== null) {
                  result0 = (function(offset2, elements) {
                    return elements.length !== 1 ? {
                      type: "sequence",
                      elements
                    } : elements[0];
                  })(pos0, result0);
                }
                if (result0 === null) {
                  pos = pos0;
                }
              }
              return result0;
            }
            function parse_labeled() {
              var result0, result1, result22;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_identifier();
              if (result0 !== null) {
                result1 = parse_colon();
                if (result1 !== null) {
                  result22 = parse_prefixed();
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, label, expression) {
                  return {
                    type: "labeled",
                    label,
                    expression
                  };
                })(pos0, result0[0], result0[2]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              if (result0 === null) {
                result0 = parse_prefixed();
              }
              return result0;
            }
            function parse_prefixed() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_and();
              if (result0 !== null) {
                result1 = parse_action();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, code) {
                  return {
                    type: "semantic_and",
                    code
                  };
                })(pos0, result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              if (result0 === null) {
                pos0 = pos;
                pos1 = pos;
                result0 = parse_and();
                if (result0 !== null) {
                  result1 = parse_suffixed();
                  if (result1 !== null) {
                    result0 = [result0, result1];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
                if (result0 !== null) {
                  result0 = /* @__PURE__ */ (function(offset2, expression) {
                    return {
                      type: "simple_and",
                      expression
                    };
                  })(pos0, result0[1]);
                }
                if (result0 === null) {
                  pos = pos0;
                }
                if (result0 === null) {
                  pos0 = pos;
                  pos1 = pos;
                  result0 = parse_not();
                  if (result0 !== null) {
                    result1 = parse_action();
                    if (result1 !== null) {
                      result0 = [result0, result1];
                    } else {
                      result0 = null;
                      pos = pos1;
                    }
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                  if (result0 !== null) {
                    result0 = /* @__PURE__ */ (function(offset2, code) {
                      return {
                        type: "semantic_not",
                        code
                      };
                    })(pos0, result0[1]);
                  }
                  if (result0 === null) {
                    pos = pos0;
                  }
                  if (result0 === null) {
                    pos0 = pos;
                    pos1 = pos;
                    result0 = parse_not();
                    if (result0 !== null) {
                      result1 = parse_suffixed();
                      if (result1 !== null) {
                        result0 = [result0, result1];
                      } else {
                        result0 = null;
                        pos = pos1;
                      }
                    } else {
                      result0 = null;
                      pos = pos1;
                    }
                    if (result0 !== null) {
                      result0 = /* @__PURE__ */ (function(offset2, expression) {
                        return {
                          type: "simple_not",
                          expression
                        };
                      })(pos0, result0[1]);
                    }
                    if (result0 === null) {
                      pos = pos0;
                    }
                    if (result0 === null) {
                      result0 = parse_suffixed();
                    }
                  }
                }
              }
              return result0;
            }
            function parse_suffixed() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_primary();
              if (result0 !== null) {
                result1 = parse_question();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, expression) {
                  return {
                    type: "optional",
                    expression
                  };
                })(pos0, result0[0]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              if (result0 === null) {
                pos0 = pos;
                pos1 = pos;
                result0 = parse_primary();
                if (result0 !== null) {
                  result1 = parse_star();
                  if (result1 !== null) {
                    result0 = [result0, result1];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
                if (result0 !== null) {
                  result0 = /* @__PURE__ */ (function(offset2, expression) {
                    return {
                      type: "zero_or_more",
                      expression
                    };
                  })(pos0, result0[0]);
                }
                if (result0 === null) {
                  pos = pos0;
                }
                if (result0 === null) {
                  pos0 = pos;
                  pos1 = pos;
                  result0 = parse_primary();
                  if (result0 !== null) {
                    result1 = parse_plus();
                    if (result1 !== null) {
                      result0 = [result0, result1];
                    } else {
                      result0 = null;
                      pos = pos1;
                    }
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                  if (result0 !== null) {
                    result0 = /* @__PURE__ */ (function(offset2, expression) {
                      return {
                        type: "one_or_more",
                        expression
                      };
                    })(pos0, result0[0]);
                  }
                  if (result0 === null) {
                    pos = pos0;
                  }
                  if (result0 === null) {
                    result0 = parse_primary();
                  }
                }
              }
              return result0;
            }
            function parse_primary() {
              var result0, result1, result22;
              var pos0, pos1, pos2, pos3;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_identifier();
              if (result0 !== null) {
                pos2 = pos;
                reportFailures++;
                pos3 = pos;
                result1 = parse_string();
                result1 = result1 !== null ? result1 : "";
                if (result1 !== null) {
                  result22 = parse_equals();
                  if (result22 !== null) {
                    result1 = [result1, result22];
                  } else {
                    result1 = null;
                    pos = pos3;
                  }
                } else {
                  result1 = null;
                  pos = pos3;
                }
                reportFailures--;
                if (result1 === null) {
                  result1 = "";
                } else {
                  result1 = null;
                  pos = pos2;
                }
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, name) {
                  return {
                    type: "rule_ref",
                    name
                  };
                })(pos0, result0[0]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              if (result0 === null) {
                result0 = parse_literal();
                if (result0 === null) {
                  pos0 = pos;
                  result0 = parse_dot();
                  if (result0 !== null) {
                    result0 = /* @__PURE__ */ (function(offset2) {
                      return { type: "any" };
                    })(pos0);
                  }
                  if (result0 === null) {
                    pos = pos0;
                  }
                  if (result0 === null) {
                    result0 = parse_class();
                    if (result0 === null) {
                      pos0 = pos;
                      pos1 = pos;
                      result0 = parse_lparen();
                      if (result0 !== null) {
                        result1 = parse_choice();
                        if (result1 !== null) {
                          result22 = parse_rparen();
                          if (result22 !== null) {
                            result0 = [result0, result1, result22];
                          } else {
                            result0 = null;
                            pos = pos1;
                          }
                        } else {
                          result0 = null;
                          pos = pos1;
                        }
                      } else {
                        result0 = null;
                        pos = pos1;
                      }
                      if (result0 !== null) {
                        result0 = /* @__PURE__ */ (function(offset2, expression) {
                          return expression;
                        })(pos0, result0[1]);
                      }
                      if (result0 === null) {
                        pos = pos0;
                      }
                    }
                  }
                }
              }
              return result0;
            }
            function parse_action() {
              var result0, result1;
              var pos0, pos1;
              reportFailures++;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_braced();
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, braced) {
                  return braced.substr(1, braced.length - 2);
                })(pos0, result0[0]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              reportFailures--;
              if (reportFailures === 0 && result0 === null) {
                matchFailed("action");
              }
              return result0;
            }
            function parse_braced() {
              var result0, result1, result22;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 123) {
                result0 = "{";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"{"');
                }
              }
              if (result0 !== null) {
                result1 = [];
                result22 = parse_braced();
                if (result22 === null) {
                  result22 = parse_nonBraceCharacter();
                }
                while (result22 !== null) {
                  result1.push(result22);
                  result22 = parse_braced();
                  if (result22 === null) {
                    result22 = parse_nonBraceCharacter();
                  }
                }
                if (result1 !== null) {
                  if (input.charCodeAt(pos) === 125) {
                    result22 = "}";
                    pos++;
                  } else {
                    result22 = null;
                    if (reportFailures === 0) {
                      matchFailed('"}"');
                    }
                  }
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, parts) {
                  return "{" + parts.join("") + "}";
                })(pos0, result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_nonBraceCharacters() {
              var result0, result1;
              var pos0;
              pos0 = pos;
              result1 = parse_nonBraceCharacter();
              if (result1 !== null) {
                result0 = [];
                while (result1 !== null) {
                  result0.push(result1);
                  result1 = parse_nonBraceCharacter();
                }
              } else {
                result0 = null;
              }
              if (result0 !== null) {
                result0 = (function(offset2, chars) {
                  return chars.join("");
                })(pos0, result0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_nonBraceCharacter() {
              var result0;
              if (/^[^{}]/.test(input.charAt(pos))) {
                result0 = input.charAt(pos);
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed("[^{}]");
                }
              }
              return result0;
            }
            function parse_equals() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 61) {
                result0 = "=";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"="');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "=";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_colon() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 58) {
                result0 = ":";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('":"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return ":";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_semicolon() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 59) {
                result0 = ";";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('";"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return ";";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_slash() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 47) {
                result0 = "/";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"/"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "/";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_and() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 38) {
                result0 = "&";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"&"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "&";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_not() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 33) {
                result0 = "!";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"!"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "!";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_question() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 63) {
                result0 = "?";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"?"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "?";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_star() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 42) {
                result0 = "*";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"*"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "*";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_plus() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 43) {
                result0 = "+";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"+"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "+";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_lparen() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 40) {
                result0 = "(";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"("');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "(";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_rparen() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 41) {
                result0 = ")";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('")"');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return ")";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_dot() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 46) {
                result0 = ".";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"."');
                }
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return ".";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_identifier() {
              var result0, result1, result22;
              var pos0, pos1;
              reportFailures++;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_letter();
              if (result0 === null) {
                if (input.charCodeAt(pos) === 95) {
                  result0 = "_";
                  pos++;
                } else {
                  result0 = null;
                  if (reportFailures === 0) {
                    matchFailed('"_"');
                  }
                }
                if (result0 === null) {
                  if (input.charCodeAt(pos) === 36) {
                    result0 = "$";
                    pos++;
                  } else {
                    result0 = null;
                    if (reportFailures === 0) {
                      matchFailed('"$"');
                    }
                  }
                }
              }
              if (result0 !== null) {
                result1 = [];
                result22 = parse_letter();
                if (result22 === null) {
                  result22 = parse_digit();
                  if (result22 === null) {
                    if (input.charCodeAt(pos) === 95) {
                      result22 = "_";
                      pos++;
                    } else {
                      result22 = null;
                      if (reportFailures === 0) {
                        matchFailed('"_"');
                      }
                    }
                    if (result22 === null) {
                      if (input.charCodeAt(pos) === 36) {
                        result22 = "$";
                        pos++;
                      } else {
                        result22 = null;
                        if (reportFailures === 0) {
                          matchFailed('"$"');
                        }
                      }
                    }
                  }
                }
                while (result22 !== null) {
                  result1.push(result22);
                  result22 = parse_letter();
                  if (result22 === null) {
                    result22 = parse_digit();
                    if (result22 === null) {
                      if (input.charCodeAt(pos) === 95) {
                        result22 = "_";
                        pos++;
                      } else {
                        result22 = null;
                        if (reportFailures === 0) {
                          matchFailed('"_"');
                        }
                      }
                      if (result22 === null) {
                        if (input.charCodeAt(pos) === 36) {
                          result22 = "$";
                          pos++;
                        } else {
                          result22 = null;
                          if (reportFailures === 0) {
                            matchFailed('"$"');
                          }
                        }
                      }
                    }
                  }
                }
                if (result1 !== null) {
                  result22 = parse___();
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, head, tail) {
                  return head + tail.join("");
                })(pos0, result0[0], result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              reportFailures--;
              if (reportFailures === 0 && result0 === null) {
                matchFailed("identifier");
              }
              return result0;
            }
            function parse_literal() {
              var result0, result1, result22;
              var pos0, pos1;
              reportFailures++;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_doubleQuotedString();
              if (result0 === null) {
                result0 = parse_singleQuotedString();
              }
              if (result0 !== null) {
                if (input.charCodeAt(pos) === 105) {
                  result1 = "i";
                  pos++;
                } else {
                  result1 = null;
                  if (reportFailures === 0) {
                    matchFailed('"i"');
                  }
                }
                result1 = result1 !== null ? result1 : "";
                if (result1 !== null) {
                  result22 = parse___();
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, value, flags) {
                  return {
                    type: "literal",
                    value,
                    ignoreCase: flags === "i"
                  };
                })(pos0, result0[0], result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              reportFailures--;
              if (reportFailures === 0 && result0 === null) {
                matchFailed("literal");
              }
              return result0;
            }
            function parse_string() {
              var result0, result1;
              var pos0, pos1;
              reportFailures++;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_doubleQuotedString();
              if (result0 === null) {
                result0 = parse_singleQuotedString();
              }
              if (result0 !== null) {
                result1 = parse___();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, string) {
                  return string;
                })(pos0, result0[0]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              reportFailures--;
              if (reportFailures === 0 && result0 === null) {
                matchFailed("string");
              }
              return result0;
            }
            function parse_doubleQuotedString() {
              var result0, result1, result22;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 34) {
                result0 = '"';
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"\\""');
                }
              }
              if (result0 !== null) {
                result1 = [];
                result22 = parse_doubleQuotedCharacter();
                while (result22 !== null) {
                  result1.push(result22);
                  result22 = parse_doubleQuotedCharacter();
                }
                if (result1 !== null) {
                  if (input.charCodeAt(pos) === 34) {
                    result22 = '"';
                    pos++;
                  } else {
                    result22 = null;
                    if (reportFailures === 0) {
                      matchFailed('"\\""');
                    }
                  }
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, chars) {
                  return chars.join("");
                })(pos0, result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_doubleQuotedCharacter() {
              var result0;
              result0 = parse_simpleDoubleQuotedCharacter();
              if (result0 === null) {
                result0 = parse_simpleEscapeSequence();
                if (result0 === null) {
                  result0 = parse_zeroEscapeSequence();
                  if (result0 === null) {
                    result0 = parse_hexEscapeSequence();
                    if (result0 === null) {
                      result0 = parse_unicodeEscapeSequence();
                      if (result0 === null) {
                        result0 = parse_eolEscapeSequence();
                      }
                    }
                  }
                }
              }
              return result0;
            }
            function parse_simpleDoubleQuotedCharacter() {
              var result0, result1;
              var pos0, pos1, pos2;
              pos0 = pos;
              pos1 = pos;
              pos2 = pos;
              reportFailures++;
              if (input.charCodeAt(pos) === 34) {
                result0 = '"';
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"\\""');
                }
              }
              if (result0 === null) {
                if (input.charCodeAt(pos) === 92) {
                  result0 = "\\";
                  pos++;
                } else {
                  result0 = null;
                  if (reportFailures === 0) {
                    matchFailed('"\\\\"');
                  }
                }
                if (result0 === null) {
                  result0 = parse_eolChar();
                }
              }
              reportFailures--;
              if (result0 === null) {
                result0 = "";
              } else {
                result0 = null;
                pos = pos2;
              }
              if (result0 !== null) {
                if (input.length > pos) {
                  result1 = input.charAt(pos);
                  pos++;
                } else {
                  result1 = null;
                  if (reportFailures === 0) {
                    matchFailed("any character");
                  }
                }
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, char_) {
                  return char_;
                })(pos0, result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_singleQuotedString() {
              var result0, result1, result22;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 39) {
                result0 = "'";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed(`"'"`);
                }
              }
              if (result0 !== null) {
                result1 = [];
                result22 = parse_singleQuotedCharacter();
                while (result22 !== null) {
                  result1.push(result22);
                  result22 = parse_singleQuotedCharacter();
                }
                if (result1 !== null) {
                  if (input.charCodeAt(pos) === 39) {
                    result22 = "'";
                    pos++;
                  } else {
                    result22 = null;
                    if (reportFailures === 0) {
                      matchFailed(`"'"`);
                    }
                  }
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, chars) {
                  return chars.join("");
                })(pos0, result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_singleQuotedCharacter() {
              var result0;
              result0 = parse_simpleSingleQuotedCharacter();
              if (result0 === null) {
                result0 = parse_simpleEscapeSequence();
                if (result0 === null) {
                  result0 = parse_zeroEscapeSequence();
                  if (result0 === null) {
                    result0 = parse_hexEscapeSequence();
                    if (result0 === null) {
                      result0 = parse_unicodeEscapeSequence();
                      if (result0 === null) {
                        result0 = parse_eolEscapeSequence();
                      }
                    }
                  }
                }
              }
              return result0;
            }
            function parse_simpleSingleQuotedCharacter() {
              var result0, result1;
              var pos0, pos1, pos2;
              pos0 = pos;
              pos1 = pos;
              pos2 = pos;
              reportFailures++;
              if (input.charCodeAt(pos) === 39) {
                result0 = "'";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed(`"'"`);
                }
              }
              if (result0 === null) {
                if (input.charCodeAt(pos) === 92) {
                  result0 = "\\";
                  pos++;
                } else {
                  result0 = null;
                  if (reportFailures === 0) {
                    matchFailed('"\\\\"');
                  }
                }
                if (result0 === null) {
                  result0 = parse_eolChar();
                }
              }
              reportFailures--;
              if (result0 === null) {
                result0 = "";
              } else {
                result0 = null;
                pos = pos2;
              }
              if (result0 !== null) {
                if (input.length > pos) {
                  result1 = input.charAt(pos);
                  pos++;
                } else {
                  result1 = null;
                  if (reportFailures === 0) {
                    matchFailed("any character");
                  }
                }
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, char_) {
                  return char_;
                })(pos0, result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_class() {
              var result0, result1, result22, result32, result4, result5;
              var pos0, pos1;
              reportFailures++;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 91) {
                result0 = "[";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"["');
                }
              }
              if (result0 !== null) {
                if (input.charCodeAt(pos) === 94) {
                  result1 = "^";
                  pos++;
                } else {
                  result1 = null;
                  if (reportFailures === 0) {
                    matchFailed('"^"');
                  }
                }
                result1 = result1 !== null ? result1 : "";
                if (result1 !== null) {
                  result22 = [];
                  result32 = parse_classCharacterRange();
                  if (result32 === null) {
                    result32 = parse_classCharacter();
                  }
                  while (result32 !== null) {
                    result22.push(result32);
                    result32 = parse_classCharacterRange();
                    if (result32 === null) {
                      result32 = parse_classCharacter();
                    }
                  }
                  if (result22 !== null) {
                    if (input.charCodeAt(pos) === 93) {
                      result32 = "]";
                      pos++;
                    } else {
                      result32 = null;
                      if (reportFailures === 0) {
                        matchFailed('"]"');
                      }
                    }
                    if (result32 !== null) {
                      if (input.charCodeAt(pos) === 105) {
                        result4 = "i";
                        pos++;
                      } else {
                        result4 = null;
                        if (reportFailures === 0) {
                          matchFailed('"i"');
                        }
                      }
                      result4 = result4 !== null ? result4 : "";
                      if (result4 !== null) {
                        result5 = parse___();
                        if (result5 !== null) {
                          result0 = [result0, result1, result22, result32, result4, result5];
                        } else {
                          result0 = null;
                          pos = pos1;
                        }
                      } else {
                        result0 = null;
                        pos = pos1;
                      }
                    } else {
                      result0 = null;
                      pos = pos1;
                    }
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, inverted, parts, flags) {
                  var partsConverted = map(parts, function(part) {
                    return part.data;
                  });
                  var rawText = "[" + inverted + map(parts, function(part) {
                    return part.rawText;
                  }).join("") + "]" + flags;
                  return {
                    type: "class",
                    inverted: inverted === "^",
                    ignoreCase: flags === "i",
                    parts: partsConverted,
                    // FIXME: Get the raw text from the input directly.
                    rawText
                  };
                })(pos0, result0[1], result0[2], result0[4]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              reportFailures--;
              if (reportFailures === 0 && result0 === null) {
                matchFailed("character class");
              }
              return result0;
            }
            function parse_classCharacterRange() {
              var result0, result1, result22;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              result0 = parse_classCharacter();
              if (result0 !== null) {
                if (input.charCodeAt(pos) === 45) {
                  result1 = "-";
                  pos++;
                } else {
                  result1 = null;
                  if (reportFailures === 0) {
                    matchFailed('"-"');
                  }
                }
                if (result1 !== null) {
                  result22 = parse_classCharacter();
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, begin, end) {
                  if (begin.data.charCodeAt(0) > end.data.charCodeAt(0)) {
                    throw new this.SyntaxError(
                      "Invalid character range: " + begin.rawText + "-" + end.rawText + "."
                    );
                  }
                  return {
                    data: [begin.data, end.data],
                    // FIXME: Get the raw text from the input directly.
                    rawText: begin.rawText + "-" + end.rawText
                  };
                })(pos0, result0[0], result0[2]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_classCharacter() {
              var result0;
              var pos0;
              pos0 = pos;
              result0 = parse_bracketDelimitedCharacter();
              if (result0 !== null) {
                result0 = (function(offset2, char_) {
                  return {
                    data: char_,
                    // FIXME: Get the raw text from the input directly.
                    rawText: quoteForRegexpClass(char_)
                  };
                })(pos0, result0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_bracketDelimitedCharacter() {
              var result0;
              result0 = parse_simpleBracketDelimitedCharacter();
              if (result0 === null) {
                result0 = parse_simpleEscapeSequence();
                if (result0 === null) {
                  result0 = parse_zeroEscapeSequence();
                  if (result0 === null) {
                    result0 = parse_hexEscapeSequence();
                    if (result0 === null) {
                      result0 = parse_unicodeEscapeSequence();
                      if (result0 === null) {
                        result0 = parse_eolEscapeSequence();
                      }
                    }
                  }
                }
              }
              return result0;
            }
            function parse_simpleBracketDelimitedCharacter() {
              var result0, result1;
              var pos0, pos1, pos2;
              pos0 = pos;
              pos1 = pos;
              pos2 = pos;
              reportFailures++;
              if (input.charCodeAt(pos) === 93) {
                result0 = "]";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"]"');
                }
              }
              if (result0 === null) {
                if (input.charCodeAt(pos) === 92) {
                  result0 = "\\";
                  pos++;
                } else {
                  result0 = null;
                  if (reportFailures === 0) {
                    matchFailed('"\\\\"');
                  }
                }
                if (result0 === null) {
                  result0 = parse_eolChar();
                }
              }
              reportFailures--;
              if (result0 === null) {
                result0 = "";
              } else {
                result0 = null;
                pos = pos2;
              }
              if (result0 !== null) {
                if (input.length > pos) {
                  result1 = input.charAt(pos);
                  pos++;
                } else {
                  result1 = null;
                  if (reportFailures === 0) {
                    matchFailed("any character");
                  }
                }
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, char_) {
                  return char_;
                })(pos0, result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_simpleEscapeSequence() {
              var result0, result1, result22;
              var pos0, pos1, pos2;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 92) {
                result0 = "\\";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"\\\\"');
                }
              }
              if (result0 !== null) {
                pos2 = pos;
                reportFailures++;
                result1 = parse_digit();
                if (result1 === null) {
                  if (input.charCodeAt(pos) === 120) {
                    result1 = "x";
                    pos++;
                  } else {
                    result1 = null;
                    if (reportFailures === 0) {
                      matchFailed('"x"');
                    }
                  }
                  if (result1 === null) {
                    if (input.charCodeAt(pos) === 117) {
                      result1 = "u";
                      pos++;
                    } else {
                      result1 = null;
                      if (reportFailures === 0) {
                        matchFailed('"u"');
                      }
                    }
                    if (result1 === null) {
                      result1 = parse_eolChar();
                    }
                  }
                }
                reportFailures--;
                if (result1 === null) {
                  result1 = "";
                } else {
                  result1 = null;
                  pos = pos2;
                }
                if (result1 !== null) {
                  if (input.length > pos) {
                    result22 = input.charAt(pos);
                    pos++;
                  } else {
                    result22 = null;
                    if (reportFailures === 0) {
                      matchFailed("any character");
                    }
                  }
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, char_) {
                  return char_.replace("b", "\b").replace("f", "\f").replace("n", "\n").replace("r", "\r").replace("t", "	").replace("v", "\v");
                })(pos0, result0[2]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_zeroEscapeSequence() {
              var result0, result1;
              var pos0, pos1, pos2;
              pos0 = pos;
              pos1 = pos;
              if (input.substr(pos, 2) === "\\0") {
                result0 = "\\0";
                pos += 2;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"\\\\0"');
                }
              }
              if (result0 !== null) {
                pos2 = pos;
                reportFailures++;
                result1 = parse_digit();
                reportFailures--;
                if (result1 === null) {
                  result1 = "";
                } else {
                  result1 = null;
                  pos = pos2;
                }
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2) {
                  return "\0";
                })(pos0);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_hexEscapeSequence() {
              var result0, result1, result22;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.substr(pos, 2) === "\\x") {
                result0 = "\\x";
                pos += 2;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"\\\\x"');
                }
              }
              if (result0 !== null) {
                result1 = parse_hexDigit();
                if (result1 !== null) {
                  result22 = parse_hexDigit();
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, h1, h2) {
                  return String.fromCharCode(parseInt(h1 + h2, 16));
                })(pos0, result0[1], result0[2]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_unicodeEscapeSequence() {
              var result0, result1, result22, result32, result4;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.substr(pos, 2) === "\\u") {
                result0 = "\\u";
                pos += 2;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"\\\\u"');
                }
              }
              if (result0 !== null) {
                result1 = parse_hexDigit();
                if (result1 !== null) {
                  result22 = parse_hexDigit();
                  if (result22 !== null) {
                    result32 = parse_hexDigit();
                    if (result32 !== null) {
                      result4 = parse_hexDigit();
                      if (result4 !== null) {
                        result0 = [result0, result1, result22, result32, result4];
                      } else {
                        result0 = null;
                        pos = pos1;
                      }
                    } else {
                      result0 = null;
                      pos = pos1;
                    }
                  } else {
                    result0 = null;
                    pos = pos1;
                  }
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = (function(offset2, h1, h2, h3, h4) {
                  return String.fromCharCode(parseInt(h1 + h2 + h3 + h4, 16));
                })(pos0, result0[1], result0[2], result0[3], result0[4]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_eolEscapeSequence() {
              var result0, result1;
              var pos0, pos1;
              pos0 = pos;
              pos1 = pos;
              if (input.charCodeAt(pos) === 92) {
                result0 = "\\";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"\\\\"');
                }
              }
              if (result0 !== null) {
                result1 = parse_eol();
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos1;
                }
              } else {
                result0 = null;
                pos = pos1;
              }
              if (result0 !== null) {
                result0 = /* @__PURE__ */ (function(offset2, eol) {
                  return eol;
                })(pos0, result0[1]);
              }
              if (result0 === null) {
                pos = pos0;
              }
              return result0;
            }
            function parse_digit() {
              var result0;
              if (/^[0-9]/.test(input.charAt(pos))) {
                result0 = input.charAt(pos);
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed("[0-9]");
                }
              }
              return result0;
            }
            function parse_hexDigit() {
              var result0;
              if (/^[0-9a-fA-F]/.test(input.charAt(pos))) {
                result0 = input.charAt(pos);
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed("[0-9a-fA-F]");
                }
              }
              return result0;
            }
            function parse_letter() {
              var result0;
              result0 = parse_lowerCaseLetter();
              if (result0 === null) {
                result0 = parse_upperCaseLetter();
              }
              return result0;
            }
            function parse_lowerCaseLetter() {
              var result0;
              if (/^[a-z]/.test(input.charAt(pos))) {
                result0 = input.charAt(pos);
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed("[a-z]");
                }
              }
              return result0;
            }
            function parse_upperCaseLetter() {
              var result0;
              if (/^[A-Z]/.test(input.charAt(pos))) {
                result0 = input.charAt(pos);
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed("[A-Z]");
                }
              }
              return result0;
            }
            function parse___() {
              var result0, result1;
              result0 = [];
              result1 = parse_whitespace();
              if (result1 === null) {
                result1 = parse_eol();
                if (result1 === null) {
                  result1 = parse_comment();
                }
              }
              while (result1 !== null) {
                result0.push(result1);
                result1 = parse_whitespace();
                if (result1 === null) {
                  result1 = parse_eol();
                  if (result1 === null) {
                    result1 = parse_comment();
                  }
                }
              }
              return result0;
            }
            function parse_comment() {
              var result0;
              reportFailures++;
              result0 = parse_singleLineComment();
              if (result0 === null) {
                result0 = parse_multiLineComment();
              }
              reportFailures--;
              if (reportFailures === 0 && result0 === null) {
                matchFailed("comment");
              }
              return result0;
            }
            function parse_singleLineComment() {
              var result0, result1, result22, result32;
              var pos0, pos1, pos2;
              pos0 = pos;
              if (input.substr(pos, 2) === "//") {
                result0 = "//";
                pos += 2;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"//"');
                }
              }
              if (result0 !== null) {
                result1 = [];
                pos1 = pos;
                pos2 = pos;
                reportFailures++;
                result22 = parse_eolChar();
                reportFailures--;
                if (result22 === null) {
                  result22 = "";
                } else {
                  result22 = null;
                  pos = pos2;
                }
                if (result22 !== null) {
                  if (input.length > pos) {
                    result32 = input.charAt(pos);
                    pos++;
                  } else {
                    result32 = null;
                    if (reportFailures === 0) {
                      matchFailed("any character");
                    }
                  }
                  if (result32 !== null) {
                    result22 = [result22, result32];
                  } else {
                    result22 = null;
                    pos = pos1;
                  }
                } else {
                  result22 = null;
                  pos = pos1;
                }
                while (result22 !== null) {
                  result1.push(result22);
                  pos1 = pos;
                  pos2 = pos;
                  reportFailures++;
                  result22 = parse_eolChar();
                  reportFailures--;
                  if (result22 === null) {
                    result22 = "";
                  } else {
                    result22 = null;
                    pos = pos2;
                  }
                  if (result22 !== null) {
                    if (input.length > pos) {
                      result32 = input.charAt(pos);
                      pos++;
                    } else {
                      result32 = null;
                      if (reportFailures === 0) {
                        matchFailed("any character");
                      }
                    }
                    if (result32 !== null) {
                      result22 = [result22, result32];
                    } else {
                      result22 = null;
                      pos = pos1;
                    }
                  } else {
                    result22 = null;
                    pos = pos1;
                  }
                }
                if (result1 !== null) {
                  result0 = [result0, result1];
                } else {
                  result0 = null;
                  pos = pos0;
                }
              } else {
                result0 = null;
                pos = pos0;
              }
              return result0;
            }
            function parse_multiLineComment() {
              var result0, result1, result22, result32;
              var pos0, pos1, pos2;
              pos0 = pos;
              if (input.substr(pos, 2) === "/*") {
                result0 = "/*";
                pos += 2;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"/*"');
                }
              }
              if (result0 !== null) {
                result1 = [];
                pos1 = pos;
                pos2 = pos;
                reportFailures++;
                if (input.substr(pos, 2) === "*/") {
                  result22 = "*/";
                  pos += 2;
                } else {
                  result22 = null;
                  if (reportFailures === 0) {
                    matchFailed('"*/"');
                  }
                }
                reportFailures--;
                if (result22 === null) {
                  result22 = "";
                } else {
                  result22 = null;
                  pos = pos2;
                }
                if (result22 !== null) {
                  if (input.length > pos) {
                    result32 = input.charAt(pos);
                    pos++;
                  } else {
                    result32 = null;
                    if (reportFailures === 0) {
                      matchFailed("any character");
                    }
                  }
                  if (result32 !== null) {
                    result22 = [result22, result32];
                  } else {
                    result22 = null;
                    pos = pos1;
                  }
                } else {
                  result22 = null;
                  pos = pos1;
                }
                while (result22 !== null) {
                  result1.push(result22);
                  pos1 = pos;
                  pos2 = pos;
                  reportFailures++;
                  if (input.substr(pos, 2) === "*/") {
                    result22 = "*/";
                    pos += 2;
                  } else {
                    result22 = null;
                    if (reportFailures === 0) {
                      matchFailed('"*/"');
                    }
                  }
                  reportFailures--;
                  if (result22 === null) {
                    result22 = "";
                  } else {
                    result22 = null;
                    pos = pos2;
                  }
                  if (result22 !== null) {
                    if (input.length > pos) {
                      result32 = input.charAt(pos);
                      pos++;
                    } else {
                      result32 = null;
                      if (reportFailures === 0) {
                        matchFailed("any character");
                      }
                    }
                    if (result32 !== null) {
                      result22 = [result22, result32];
                    } else {
                      result22 = null;
                      pos = pos1;
                    }
                  } else {
                    result22 = null;
                    pos = pos1;
                  }
                }
                if (result1 !== null) {
                  if (input.substr(pos, 2) === "*/") {
                    result22 = "*/";
                    pos += 2;
                  } else {
                    result22 = null;
                    if (reportFailures === 0) {
                      matchFailed('"*/"');
                    }
                  }
                  if (result22 !== null) {
                    result0 = [result0, result1, result22];
                  } else {
                    result0 = null;
                    pos = pos0;
                  }
                } else {
                  result0 = null;
                  pos = pos0;
                }
              } else {
                result0 = null;
                pos = pos0;
              }
              return result0;
            }
            function parse_eol() {
              var result0;
              reportFailures++;
              if (input.charCodeAt(pos) === 10) {
                result0 = "\n";
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed('"\\n"');
                }
              }
              if (result0 === null) {
                if (input.substr(pos, 2) === "\r\n") {
                  result0 = "\r\n";
                  pos += 2;
                } else {
                  result0 = null;
                  if (reportFailures === 0) {
                    matchFailed('"\\r\\n"');
                  }
                }
                if (result0 === null) {
                  if (input.charCodeAt(pos) === 13) {
                    result0 = "\r";
                    pos++;
                  } else {
                    result0 = null;
                    if (reportFailures === 0) {
                      matchFailed('"\\r"');
                    }
                  }
                  if (result0 === null) {
                    if (input.charCodeAt(pos) === 8232) {
                      result0 = "\u2028";
                      pos++;
                    } else {
                      result0 = null;
                      if (reportFailures === 0) {
                        matchFailed('"\\u2028"');
                      }
                    }
                    if (result0 === null) {
                      if (input.charCodeAt(pos) === 8233) {
                        result0 = "\u2029";
                        pos++;
                      } else {
                        result0 = null;
                        if (reportFailures === 0) {
                          matchFailed('"\\u2029"');
                        }
                      }
                    }
                  }
                }
              }
              reportFailures--;
              if (reportFailures === 0 && result0 === null) {
                matchFailed("end of line");
              }
              return result0;
            }
            function parse_eolChar() {
              var result0;
              if (/^[\n\r\u2028\u2029]/.test(input.charAt(pos))) {
                result0 = input.charAt(pos);
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed("[\\n\\r\\u2028\\u2029]");
                }
              }
              return result0;
            }
            function parse_whitespace() {
              var result0;
              reportFailures++;
              if (/^[ \t\x0B\f\xA0\uFEFF\u1680\u180E\u2000-\u200A\u202F\u205F\u3000]/.test(input.charAt(pos))) {
                result0 = input.charAt(pos);
                pos++;
              } else {
                result0 = null;
                if (reportFailures === 0) {
                  matchFailed("[ \\t\\x0B\\f\\xA0\\uFEFF\\u1680\\u180E\\u2000-\\u200A\\u202F\\u205F\\u3000]");
                }
              }
              reportFailures--;
              if (reportFailures === 0 && result0 === null) {
                matchFailed("whitespace");
              }
              return result0;
            }
            function cleanupExpected(expected) {
              expected.sort();
              var lastExpected = null;
              var cleanExpected = [];
              for (var i = 0; i < expected.length; i++) {
                if (expected[i] !== lastExpected) {
                  cleanExpected.push(expected[i]);
                  lastExpected = expected[i];
                }
              }
              return cleanExpected;
            }
            function computeErrorPosition() {
              var line = 1;
              var column = 1;
              var seenCR = false;
              for (var i = 0; i < Math.max(pos, rightmostFailuresPos); i++) {
                var ch = input.charAt(i);
                if (ch === "\n") {
                  if (!seenCR) {
                    line++;
                  }
                  column = 1;
                  seenCR = false;
                } else if (ch === "\r" || ch === "\u2028" || ch === "\u2029") {
                  line++;
                  column = 1;
                  seenCR = true;
                } else {
                  column++;
                  seenCR = false;
                }
              }
              return { line, column };
            }
            var result3 = parseFunctions[startRule]();
            if (result3 === null || pos !== input.length) {
              var offset = Math.max(pos, rightmostFailuresPos);
              var found = offset < input.length ? input.charAt(offset) : null;
              var errorPosition = computeErrorPosition();
              throw new this.SyntaxError(
                cleanupExpected(rightmostFailuresExpected),
                found,
                offset,
                errorPosition.line,
                errorPosition.column
              );
            }
            return result3;
          },
          /* Returns the parser source code. */
          toSource: function() {
            return this._source;
          }
        };
        result2.SyntaxError = function(expected, found, offset, line, column) {
          function buildMessage(expected2, found2) {
            var expectedHumanized, foundHumanized;
            switch (expected2.length) {
              case 0:
                expectedHumanized = "end of input";
                break;
              case 1:
                expectedHumanized = expected2[0];
                break;
              default:
                expectedHumanized = expected2.slice(0, expected2.length - 1).join(", ") + " or " + expected2[expected2.length - 1];
            }
            foundHumanized = found2 ? quote2(found2) : "end of input";
            return "Expected " + expectedHumanized + " but " + foundHumanized + " found.";
          }
          this.name = "SyntaxError";
          this.expected = expected;
          this.found = found;
          this.message = buildMessage(expected, found);
          this.offset = offset;
          this.line = line;
          this.column = column;
        };
        result2.SyntaxError.prototype = Error.prototype;
        return result2;
      })();
      PEG.compiler = {
        /*
         * Names of passes that will get run during the compilation (in the specified
         * order).
         */
        appliedPassNames: [
          "reportMissingRules",
          "reportLeftRecursion",
          "removeProxyRules",
          "computeVarNames",
          "computeParams"
        ],
        /*
         * Generates a parser from a specified grammar AST. Throws |PEG.GrammarError|
         * if the AST contains a semantic error. Note that not all errors are detected
         * during the generation and some may protrude to the generated parser and
         * cause its malfunction.
         */
        compile: function(ast, options) {
          var that = this;
          each(this.appliedPassNames, function(passName) {
            that.passes[passName](ast);
          });
          var source = this.emitter(ast, options);
          var result = eval(source);
          result._source = source;
          return result;
        }
      };
      PEG.compiler.passes = {
        /* Checks that all referenced rules exist. */
        reportMissingRules: function(ast2) {
          function nop() {
          }
          function checkExpression(node) {
            check(node.expression);
          }
          function checkSubnodes(propertyName) {
            return function(node) {
              each(node[propertyName], check);
            };
          }
          var check = buildNodeVisitor({
            grammar: checkSubnodes("rules"),
            rule: checkExpression,
            choice: checkSubnodes("alternatives"),
            sequence: checkSubnodes("elements"),
            labeled: checkExpression,
            simple_and: checkExpression,
            simple_not: checkExpression,
            semantic_and: nop,
            semantic_not: nop,
            optional: checkExpression,
            zero_or_more: checkExpression,
            one_or_more: checkExpression,
            action: checkExpression,
            rule_ref: function(node) {
              if (!findRuleByName(ast2, node.name)) {
                throw new PEG.GrammarError(
                  'Referenced rule "' + node.name + '" does not exist.'
                );
              }
            },
            literal: nop,
            any: nop,
            "class": nop
          });
          check(ast2);
        },
        /* Checks that no left recursion is present. */
        reportLeftRecursion: function(ast2) {
          function nop() {
          }
          function checkExpression(node, appliedRules) {
            check(node.expression, appliedRules);
          }
          function checkSubnodes(propertyName) {
            return function(node, appliedRules) {
              each(node[propertyName], function(subnode) {
                check(subnode, appliedRules);
              });
            };
          }
          var check = buildNodeVisitor({
            grammar: checkSubnodes("rules"),
            rule: function(node, appliedRules) {
              check(node.expression, appliedRules.concat(node.name));
            },
            choice: checkSubnodes("alternatives"),
            sequence: function(node, appliedRules) {
              if (node.elements.length > 0) {
                check(node.elements[0], appliedRules);
              }
            },
            labeled: checkExpression,
            simple_and: checkExpression,
            simple_not: checkExpression,
            semantic_and: nop,
            semantic_not: nop,
            optional: checkExpression,
            zero_or_more: checkExpression,
            one_or_more: checkExpression,
            action: checkExpression,
            rule_ref: function(node, appliedRules) {
              if (contains(appliedRules, node.name)) {
                throw new PEG.GrammarError(
                  'Left recursion detected for rule "' + node.name + '".'
                );
              }
              check(findRuleByName(ast2, node.name), appliedRules);
            },
            literal: nop,
            any: nop,
            "class": nop
          });
          check(ast2, []);
        },
        /*
         * Removes proxy rules -- that is, rules that only delegate to other rule.
         */
        removeProxyRules: function(ast2) {
          function isProxyRule(node) {
            return node.type === "rule" && node.expression.type === "rule_ref";
          }
          function replaceRuleRefs(ast3, from, to) {
            function nop() {
            }
            function replaceInExpression(node, from2, to2) {
              replace(node.expression, from2, to2);
            }
            function replaceInSubnodes(propertyName) {
              return function(node, from2, to2) {
                each(node[propertyName], function(subnode) {
                  replace(subnode, from2, to2);
                });
              };
            }
            var replace = buildNodeVisitor({
              grammar: replaceInSubnodes("rules"),
              rule: replaceInExpression,
              choice: replaceInSubnodes("alternatives"),
              sequence: replaceInSubnodes("elements"),
              labeled: replaceInExpression,
              simple_and: replaceInExpression,
              simple_not: replaceInExpression,
              semantic_and: nop,
              semantic_not: nop,
              optional: replaceInExpression,
              zero_or_more: replaceInExpression,
              one_or_more: replaceInExpression,
              action: replaceInExpression,
              rule_ref: function(node, from2, to2) {
                if (node.name === from2) {
                  node.name = to2;
                }
              },
              literal: nop,
              any: nop,
              "class": nop
            });
            replace(ast3, from, to);
          }
          var indices = [];
          each(ast2.rules, function(rule, i) {
            if (isProxyRule(rule)) {
              replaceRuleRefs(ast2, rule.name, rule.expression.name);
              if (rule.name === ast2.startRule) {
                ast2.startRule = rule.expression.name;
              }
              indices.push(i);
            }
          });
          indices.reverse();
          each(indices, function(index) {
            ast2.rules.splice(index, 1);
          });
        },
        /*
         * Computes names of variables used for storing match results and parse
         * positions in generated code. These variables are organized as two stacks.
         * The following will hold after running this pass:
         *
         *   * All nodes except "grammar" and "rule" nodes will have a |resultVar|
         *     property. It will contain a name of the variable that will store a
         *     match result of the expression represented by the node in generated
         *     code.
         *
         *   * Some nodes will have a |posVar| property. It will contain a name of the
         *     variable that will store a parse position in generated code.
         *
         *   * All "rule" nodes will contain |resultVars| and |posVars| properties.
         *     They will contain a list of values of |resultVar| and |posVar|
         *     properties used in rule's subnodes. (This is useful to declare
         *     variables in generated code.)
         */
        computeVarNames: function(ast2) {
          function resultVar(index) {
            return "result" + index;
          }
          function posVar(index) {
            return "pos" + index;
          }
          function computeLeaf(node, index) {
            node.resultVar = resultVar(index.result);
            return { result: 0, pos: 0 };
          }
          function computeFromExpression(delta) {
            return function(node, index) {
              var depth = compute(
                node.expression,
                {
                  result: index.result + delta.result,
                  pos: index.pos + delta.pos
                }
              );
              node.resultVar = resultVar(index.result);
              if (delta.pos !== 0) {
                node.posVar = posVar(index.pos);
              }
              return {
                result: depth.result + delta.result,
                pos: depth.pos + delta.pos
              };
            };
          }
          var compute = buildNodeVisitor({
            grammar: function(node, index) {
              each(node.rules, function(node2) {
                compute(node2, index);
              });
            },
            rule: function(node, index) {
              var depth = compute(node.expression, index);
              node.resultVar = resultVar(index.result);
              node.resultVars = map(range(depth.result + 1), resultVar);
              node.posVars = map(range(depth.pos), posVar);
            },
            choice: function(node, index) {
              var depths = map(node.alternatives, function(alternative) {
                return compute(alternative, index);
              });
              node.resultVar = resultVar(index.result);
              return {
                result: Math.max.apply(null, pluck(depths, "result")),
                pos: Math.max.apply(null, pluck(depths, "pos"))
              };
            },
            sequence: function(node, index) {
              var depths = map(node.elements, function(element, i) {
                return compute(
                  element,
                  { result: index.result + i, pos: index.pos + 1 }
                );
              });
              node.resultVar = resultVar(index.result);
              node.posVar = posVar(index.pos);
              return {
                result: node.elements.length > 0 ? Math.max.apply(
                  null,
                  map(depths, function(d, i) {
                    return i + d.result;
                  })
                ) : 0,
                pos: node.elements.length > 0 ? 1 + Math.max.apply(null, pluck(depths, "pos")) : 1
              };
            },
            labeled: computeFromExpression({ result: 0, pos: 0 }),
            simple_and: computeFromExpression({ result: 0, pos: 1 }),
            simple_not: computeFromExpression({ result: 0, pos: 1 }),
            semantic_and: computeLeaf,
            semantic_not: computeLeaf,
            optional: computeFromExpression({ result: 0, pos: 0 }),
            zero_or_more: computeFromExpression({ result: 1, pos: 0 }),
            one_or_more: computeFromExpression({ result: 1, pos: 0 }),
            action: computeFromExpression({ result: 0, pos: 1 }),
            rule_ref: computeLeaf,
            literal: computeLeaf,
            any: computeLeaf,
            "class": computeLeaf
          });
          compute(ast2, { result: 0, pos: 0 });
        },
        /*
         * This pass walks through the AST and tracks what labels are visible at each
         * point. For "action", "semantic_and" and "semantic_or" nodes it computes
         * parameter names and values for the function used in generated code. (In the
         * emitter, user's code is wrapped into a function that is immediately
         * executed. Its parameter names correspond to visible labels and its
         * parameter values to their captured values). Implicitly, this pass defines
         * scoping rules for labels.
         *
         * After running this pass, all "action", "semantic_and" and "semantic_or"
         * nodes will have a |params| property containing an object mapping parameter
         * names to the expressions that will be used as their values.
         */
        computeParams: function(ast2) {
          var envs = [];
          function scoped(f) {
            envs.push({});
            f();
            envs.pop();
          }
          function nop() {
          }
          function computeForScopedExpression(node) {
            scoped(function() {
              compute(node.expression);
            });
          }
          function computeParams(node) {
            var env = envs[envs.length - 1], params = {}, name;
            for (name in env) {
              params[name] = env[name];
            }
            node.params = params;
          }
          var compute = buildNodeVisitor({
            grammar: function(node) {
              each(node.rules, compute);
            },
            rule: computeForScopedExpression,
            choice: function(node) {
              scoped(function() {
                each(node.alternatives, compute);
              });
            },
            sequence: function(node) {
              var env = envs[envs.length - 1], name;
              function fixup(name2) {
                each(pluck(node.elements, "resultVar"), function(resultVar, i) {
                  if (new RegExp("^" + resultVar + "(\\[\\d+\\])*$").test(env[name2])) {
                    env[name2] = node.resultVar + "[" + i + "]" + env[name2].substr(resultVar.length);
                  }
                });
              }
              each(node.elements, compute);
              for (name in env) {
                fixup(name);
              }
            },
            labeled: function(node) {
              envs[envs.length - 1][node.label] = node.resultVar;
              scoped(function() {
                compute(node.expression);
              });
            },
            simple_and: computeForScopedExpression,
            simple_not: computeForScopedExpression,
            semantic_and: computeParams,
            semantic_not: computeParams,
            optional: computeForScopedExpression,
            zero_or_more: computeForScopedExpression,
            one_or_more: computeForScopedExpression,
            action: function(node) {
              scoped(function() {
                compute(node.expression);
                computeParams(node);
              });
            },
            rule_ref: nop,
            literal: nop,
            any: nop,
            "class": nop
          });
          compute(ast2);
        }
      };
      PEG.compiler.emitter = function(ast2, options2) {
        options2 = options2 || {};
        if (options2.cache === undefined) {
          options2.cache = false;
        }
        if (options2.trackLineAndColumn === undefined) {
          options2.trackLineAndColumn = false;
        }
        var Codie = /* @__PURE__ */ (function(undefined2) {
          function stringEscape(s) {
            function hex(ch) {
              return ch.charCodeAt(0).toString(16).toUpperCase();
            }
            return s.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\x08/g, "\\b").replace(/\t/g, "\\t").replace(/\n/g, "\\n").replace(/\f/g, "\\f").replace(/\r/g, "\\r").replace(/[\x00-\x07\x0B\x0E\x0F]/g, function(ch) {
              return "\\x0" + hex(ch);
            }).replace(/[\x10-\x1F\x80-\xFF]/g, function(ch) {
              return "\\x" + hex(ch);
            }).replace(/[\u0180-\u0FFF]/g, function(ch) {
              return "\\u0" + hex(ch);
            }).replace(/[\u1080-\uFFFF]/g, function(ch) {
              return "\\u" + hex(ch);
            });
          }
          function push(s) {
            return "__p.push(" + s + ");";
          }
          function pushRaw(template, length, state) {
            function unindent(code, level, unindentFirst) {
              return code.replace(
                new RegExp("^.{" + level + "}", "gm"),
                function(str, offset) {
                  if (offset === 0) {
                    return unindentFirst ? "" : str;
                  } else {
                    return "";
                  }
                }
              );
            }
            var escaped = stringEscape(unindent(
              template.substring(0, length),
              state.indentLevel(),
              state.atBOL
            ));
            return escaped.length > 0 ? push('"' + escaped + '"') : "";
          }
          var Codie2 = {
            /* Codie version (uses semantic versioning). */
            VERSION: "1.1.0",
            /*
             * Specifies by how many characters do #if/#else and #for unindent their
             * content in the generated code.
             */
            indentStep: 2,
            /* Description of #-commands. Extend to define your own commands. */
            commands: {
              "if": {
                params: /^(.*)$/,
                compile: function(state, prefix, params) {
                  return ["if(" + params[0] + "){", []];
                },
                stackOp: "push"
              },
              "else": {
                params: /^$/,
                compile: function(state) {
                  var stack = state.commandStack, insideElse = stack[stack.length - 1] === "else", insideIf = stack[stack.length - 1] === "if";
                  if (insideElse) {
                    throw new Error("Multiple #elses.");
                  }
                  if (!insideIf) {
                    throw new Error("Using #else outside of #if.");
                  }
                  return ["}else{", []];
                },
                stackOp: "replace"
              },
              "for": {
                params: /^([a-zA-Z_][a-zA-Z0-9_]*)[ \t]+in[ \t]+(.*)$/,
                init: function(state) {
                  state.forCurrLevel = 0;
                  state.forMaxLevel = 0;
                },
                compile: function(state, prefix, params) {
                  var c = "__c" + state.forCurrLevel, l = "__l" + state.forCurrLevel, i = "__i" + state.forCurrLevel;
                  state.forCurrLevel++;
                  if (state.forMaxLevel < state.forCurrLevel) {
                    state.forMaxLevel = state.forCurrLevel;
                  }
                  return [
                    c + "=" + params[1] + ";" + l + "=" + c + ".length;for(" + i + "=0;" + i + "<" + l + ";" + i + "++){" + params[0] + "=" + c + "[" + i + "];",
                    [params[0], c, l, i]
                  ];
                },
                exit: function(state) {
                  state.forCurrLevel--;
                },
                stackOp: "push"
              },
              "end": {
                params: /^$/,
                compile: function(state) {
                  var stack = state.commandStack, exit;
                  if (stack.length === 0) {
                    throw new Error("Too many #ends.");
                  }
                  exit = Codie2.commands[stack[stack.length - 1]].exit;
                  if (exit) {
                    exit(state);
                  }
                  return ["}", []];
                },
                stackOp: "pop"
              },
              "block": {
                params: /^(.*)$/,
                compile: function(state, prefix, params) {
                  var x = "__x", n = "__n", l = "__l", i = "__i";
                  return [
                    x + '="' + stringEscape(prefix.substring(state.indentLevel())) + '";' + n + "=(" + params[0] + ').toString().split("\\n");' + l + "=" + n + ".length;for(" + i + "=0;" + i + "<" + l + ";" + i + "++){" + n + "[" + i + "]=" + x + "+" + n + "[" + i + ']+"\\n";}' + push(n + '.join("")'),
                    [x, n, l, i]
                  ];
                },
                stackOp: "nop"
              }
            },
            /*
             * Compiles a template into a function. When called, this function will
             * execute the template in the context of an object passed in a parameter and
             * return the result.
             */
            template: function(template) {
              var stackOps = {
                push: function(stack, name2) {
                  stack.push(name2);
                },
                replace: function(stack, name2) {
                  stack[stack.length - 1] = name2;
                },
                pop: function(stack) {
                  stack.pop();
                },
                nop: function() {
                }
              };
              function compileExpr(state2, expr) {
                state2.atBOL = false;
                return [push(expr), []];
              }
              function compileCommand(state2, prefix, name2, params) {
                var command, match2, result3;
                command = Codie2.commands[name2];
                if (!command) {
                  throw new Error("Unknown command: #" + name2 + ".");
                }
                match2 = command.params.exec(params);
                if (match2 === null) {
                  throw new Error(
                    "Invalid params for command #" + name2 + ": " + params + "."
                  );
                }
                result3 = command.compile(state2, prefix, match2.slice(1));
                stackOps[command.stackOp](state2.commandStack, name2);
                state2.atBOL = true;
                return result3;
              }
              var state = {
                // compilation state
                commandStack: [],
                //   stack of commands as they were nested
                atBOL: true,
                //   is the next character to process at BOL?
                indentLevel: function() {
                  return Codie2.indentStep * this.commandStack.length;
                }
              }, code = "", vars = ["__p=[]"], name, match, result2, i;
              for (name in Codie2.commands) {
                if (Codie2.commands[name].init) {
                  Codie2.commands[name].init(state);
                }
              }
              while ((match = /^([ \t]*)#([a-zA-Z_][a-zA-Z0-9_]*)(?:[ \t]+([^ \t\n][^\n]*))?[ \t]*(?:\n|$)|#\{([^}]*)\}/m.exec(template)) !== null) {
                code += pushRaw(template, match.index, state);
                result2 = match[2] !== undefined2 && match[2] !== "" ? compileCommand(state, match[1], match[2], match[3] || "") : compileExpr(state, match[4]);
                code += result2[0];
                vars = vars.concat(result2[1]);
                template = template.substring(match.index + match[0].length);
              }
              code += pushRaw(template, template.length, state);
              if (state.commandStack.length > 0) {
                throw new Error("Missing #end.");
              }
              vars.sort();
              for (i = 0; i < vars.length; i++) {
                if (vars[i] === vars[i - 1]) {
                  vars.splice(i--, 1);
                }
              }
              return new Function("__v", [
                "__v=__v||{};",
                "var " + vars.join(",") + ";",
                "with(__v){",
                code,
                'return __p.join("").replace(/^\\n+|\\n+$/g,"");};'
              ].join(""));
            }
          };
          return Codie2;
        })();
        var templates = (function() {
          var name, templates2 = {}, sources = {
            grammar: [
              "(function(){",
              "  /*",
              "   * Generated by PEG.js 0.7.0.",
              "   *",
              "   * http://pegjs.majda.cz/",
              "   */",
              "  ",
              /* This needs to be in sync with |quote| in utils.js. */
              "  function quote(s) {",
              "    /*",
              "     * ECMA-262, 5th ed., 7.8.4: All characters may appear literally in a",
              "     * string literal except for the closing quote character, backslash,",
              "     * carriage return, line separator, paragraph separator, and line feed.",
              "     * Any character may appear in the form of an escape sequence.",
              "     *",
              "     * For portability, we also escape escape all control and non-ASCII",
              '     * characters. Note that "\\0" and "\\v" escape sequences are not used',
              "     * because JSHint does not like the first and IE the second.",
              "     */",
              `     return '"' + s`,
              "      .replace(/\\\\/g, '\\\\\\\\')  // backslash",
              `      .replace(/"/g, '\\\\"')    // closing quote character`,
              "      .replace(/\\x08/g, '\\\\b') // backspace",
              "      .replace(/\\t/g, '\\\\t')   // horizontal tab",
              "      .replace(/\\n/g, '\\\\n')   // line feed",
              "      .replace(/\\f/g, '\\\\f')   // form feed",
              "      .replace(/\\r/g, '\\\\r')   // carriage return",
              "      .replace(/[\\x00-\\x07\\x0B\\x0E-\\x1F\\x80-\\uFFFF]/g, escape)",
              `      + '"';`,
              "  }",
              "  ",
              "  var result = {",
              "    /*",
              "     * Parses the input with a generated parser. If the parsing is successfull,",
              "     * returns a value explicitly or implicitly specified by the grammar from",
              "     * which the parser was generated (see |PEG.buildParser|). If the parsing is",
              "     * unsuccessful, throws |PEG.parser.SyntaxError| describing the error.",
              "     */",
              "    parse: function(input, startRule) {",
              "      var parseFunctions = {",
              "        #for rule in node.rules",
              '          #{string(rule.name) + ": parse_" + rule.name + (rule !== node.rules[node.rules.length - 1] ? "," : "")}',
              "        #end",
              "      };",
              "      ",
              "      if (startRule !== undefined) {",
              "        if (parseFunctions[startRule] === undefined) {",
              '          throw new Error("Invalid rule name: " + quote(startRule) + ".");',
              "        }",
              "      } else {",
              "        startRule = #{string(node.startRule)};",
              "      }",
              "      ",
              '      #{posInit("pos")};',
              "      var reportFailures = 0;",
              // 0 = report, anything > 0 = do not report
              '      #{posInit("rightmostFailuresPos")};',
              "      var rightmostFailuresExpected = [];",
              "      #if options.cache",
              "        var cache = {};",
              "      #end",
              "      ",
              /* This needs to be in sync with |padLeft| in utils.js. */
              "      function padLeft(input, padding, length) {",
              "        var result = input;",
              "        ",
              "        var padLength = length - input.length;",
              "        for (var i = 0; i < padLength; i++) {",
              "          result = padding + result;",
              "        }",
              "        ",
              "        return result;",
              "      }",
              "      ",
              /* This needs to be in sync with |escape| in utils.js. */
              "      function escape(ch) {",
              "        var charCode = ch.charCodeAt(0);",
              "        var escapeChar;",
              "        var length;",
              "        ",
              "        if (charCode <= 0xFF) {",
              "          escapeChar = 'x';",
              "          length = 2;",
              "        } else {",
              "          escapeChar = 'u';",
              "          length = 4;",
              "        }",
              "        ",
              "        return '\\\\' + escapeChar + padLeft(charCode.toString(16).toUpperCase(), '0', length);",
              "      }",
              "      ",
              "      #if options.trackLineAndColumn",
              "        function clone(object) {",
              "          var result = {};",
              "          for (var key in object) {",
              "            result[key] = object[key];",
              "          }",
              "          return result;",
              "        }",
              "        ",
              "        function advance(pos, n) {",
              "          var endOffset = pos.offset + n;",
              "          ",
              "          for (var offset = pos.offset; offset < endOffset; offset++) {",
              "            var ch = input.charAt(offset);",
              '            if (ch === "\\n") {',
              "              if (!pos.seenCR) { pos.line++; }",
              "              pos.column = 1;",
              "              pos.seenCR = false;",
              '            } else if (ch === "\\r" || ch === "\\u2028" || ch === "\\u2029") {',
              "              pos.line++;",
              "              pos.column = 1;",
              "              pos.seenCR = true;",
              "            } else {",
              "              pos.column++;",
              "              pos.seenCR = false;",
              "            }",
              "          }",
              "          ",
              "          pos.offset += n;",
              "        }",
              "        ",
              "      #end",
              "      function matchFailed(failure) {",
              '        if (#{posOffset("pos")} < #{posOffset("rightmostFailuresPos")}) {',
              "          return;",
              "        }",
              "        ",
              '        if (#{posOffset("pos")} > #{posOffset("rightmostFailuresPos")}) {',
              '          rightmostFailuresPos = #{posClone("pos")};',
              "          rightmostFailuresExpected = [];",
              "        }",
              "        ",
              "        rightmostFailuresExpected.push(failure);",
              "      }",
              "      ",
              "      #for rule in node.rules",
              "        #block emit(rule)",
              "        ",
              "      #end",
              "      ",
              "      function cleanupExpected(expected) {",
              "        expected.sort();",
              "        ",
              "        var lastExpected = null;",
              "        var cleanExpected = [];",
              "        for (var i = 0; i < expected.length; i++) {",
              "          if (expected[i] !== lastExpected) {",
              "            cleanExpected.push(expected[i]);",
              "            lastExpected = expected[i];",
              "          }",
              "        }",
              "        return cleanExpected;",
              "      }",
              "      ",
              "      #if !options.trackLineAndColumn",
              "        function computeErrorPosition() {",
              "          /*",
              "           * The first idea was to use |String.split| to break the input up to the",
              "           * error position along newlines and derive the line and column from",
              "           * there. However IE's |split| implementation is so broken that it was",
              "           * enough to prevent it.",
              "           */",
              "          ",
              "          var line = 1;",
              "          var column = 1;",
              "          var seenCR = false;",
              "          ",
              "          for (var i = 0; i < Math.max(pos, rightmostFailuresPos); i++) {",
              "            var ch = input.charAt(i);",
              '            if (ch === "\\n") {',
              "              if (!seenCR) { line++; }",
              "              column = 1;",
              "              seenCR = false;",
              '            } else if (ch === "\\r" || ch === "\\u2028" || ch === "\\u2029") {',
              "              line++;",
              "              column = 1;",
              "              seenCR = true;",
              "            } else {",
              "              column++;",
              "              seenCR = false;",
              "            }",
              "          }",
              "          ",
              "          return { line: line, column: column };",
              "        }",
              "      #end",
              "      ",
              "      #if node.initializer",
              "        #block emit(node.initializer)",
              "      #end",
              "      ",
              "      var result = parseFunctions[startRule]();",
              "      ",
              "      /*",
              "       * The parser is now in one of the following three states:",
              "       *",
              "       * 1. The parser successfully parsed the whole input.",
              "       *",
              "       *    - |result !== null|",
              '       *    - |#{posOffset("pos")} === input.length|',
              "       *    - |rightmostFailuresExpected| may or may not contain something",
              "       *",
              "       * 2. The parser successfully parsed only a part of the input.",
              "       *",
              "       *    - |result !== null|",
              '       *    - |#{posOffset("pos")} < input.length|',
              "       *    - |rightmostFailuresExpected| may or may not contain something",
              "       *",
              "       * 3. The parser did not successfully parse any part of the input.",
              "       *",
              "       *   - |result === null|",
              '       *   - |#{posOffset("pos")} === 0|',
              "       *   - |rightmostFailuresExpected| contains at least one failure",
              "       *",
              "       * All code following this comment (including called functions) must",
              "       * handle these states.",
              "       */",
              '      if (result === null || #{posOffset("pos")} !== input.length) {',
              '        var offset = Math.max(#{posOffset("pos")}, #{posOffset("rightmostFailuresPos")});',
              "        var found = offset < input.length ? input.charAt(offset) : null;",
              "        #if options.trackLineAndColumn",
              '          var errorPosition = #{posOffset("pos")} > #{posOffset("rightmostFailuresPos")} ? pos : rightmostFailuresPos;',
              "        #else",
              "          var errorPosition = computeErrorPosition();",
              "        #end",
              "        ",
              "        throw new this.SyntaxError(",
              "          cleanupExpected(rightmostFailuresExpected),",
              "          found,",
              "          offset,",
              "          errorPosition.line,",
              "          errorPosition.column",
              "        );",
              "      }",
              "      ",
              "      return result;",
              "    },",
              "    ",
              "    /* Returns the parser source code. */",
              "    toSource: function() { return this._source; }",
              "  };",
              "  ",
              "  /* Thrown when a parser encounters a syntax error. */",
              "  ",
              "  result.SyntaxError = function(expected, found, offset, line, column) {",
              "    function buildMessage(expected, found) {",
              "      var expectedHumanized, foundHumanized;",
              "      ",
              "      switch (expected.length) {",
              "        case 0:",
              '          expectedHumanized = "end of input";',
              "          break;",
              "        case 1:",
              "          expectedHumanized = expected[0];",
              "          break;",
              "        default:",
              '          expectedHumanized = expected.slice(0, expected.length - 1).join(", ")',
              '            + " or "',
              "            + expected[expected.length - 1];",
              "      }",
              "      ",
              '      foundHumanized = found ? quote(found) : "end of input";',
              "      ",
              '      return "Expected " + expectedHumanized + " but " + foundHumanized + " found.";',
              "    }",
              "    ",
              '    this.name = "SyntaxError";',
              "    this.expected = expected;",
              "    this.found = found;",
              "    this.message = buildMessage(expected, found);",
              "    this.offset = offset;",
              "    this.line = line;",
              "    this.column = column;",
              "  };",
              "  ",
              "  result.SyntaxError.prototype = Error.prototype;",
              "  ",
              "  return result;",
              "})()"
            ],
            rule: [
              "function parse_#{node.name}() {",
              "  #if options.cache",
              '    var cacheKey = "#{node.name}@" + #{posOffset("pos")};',
              "    var cachedResult = cache[cacheKey];",
              "    if (cachedResult) {",
              '      pos = #{posClone("cachedResult.nextPos")};',
              "      return cachedResult.result;",
              "    }",
              "    ",
              "  #end",
              "  #if node.resultVars.length > 0",
              '    var #{node.resultVars.join(", ")};',
              "  #end",
              "  #if node.posVars.length > 0",
              '    var #{node.posVars.join(", ")};',
              "  #end",
              "  ",
              "  #if node.displayName !== null",
              "    reportFailures++;",
              "  #end",
              "  #block emit(node.expression)",
              "  #if node.displayName !== null",
              "    reportFailures--;",
              "    if (reportFailures === 0 && #{node.resultVar} === null) {",
              "      matchFailed(#{string(node.displayName)});",
              "    }",
              "  #end",
              "  #if options.cache",
              "    ",
              "    cache[cacheKey] = {",
              '      nextPos: #{posClone("pos")},',
              "      result:  #{node.resultVar}",
              "    };",
              "  #end",
              "  return #{node.resultVar};",
              "}"
            ],
            choice: [
              "#block emit(alternative)",
              "#block nextAlternativesCode"
            ],
            "choice.next": [
              "if (#{node.resultVar} === null) {",
              "  #block code",
              "}"
            ],
            sequence: [
              "#{posSave(node)};",
              "#block code"
            ],
            "sequence.iteration": [
              "#block emit(element)",
              "if (#{element.resultVar} !== null) {",
              "  #block code",
              "} else {",
              "  #{node.resultVar} = null;",
              "  #{posRestore(node)};",
              "}"
            ],
            "sequence.inner": [
              '#{node.resultVar} = [#{pluck(node.elements, "resultVar").join(", ")}];'
            ],
            simple_and: [
              "#{posSave(node)};",
              "reportFailures++;",
              "#block emit(node.expression)",
              "reportFailures--;",
              "if (#{node.resultVar} !== null) {",
              '  #{node.resultVar} = "";',
              "  #{posRestore(node)};",
              "} else {",
              "  #{node.resultVar} = null;",
              "}"
            ],
            simple_not: [
              "#{posSave(node)};",
              "reportFailures++;",
              "#block emit(node.expression)",
              "reportFailures--;",
              "if (#{node.resultVar} === null) {",
              '  #{node.resultVar} = "";',
              "} else {",
              "  #{node.resultVar} = null;",
              "  #{posRestore(node)};",
              "}"
            ],
            semantic_and: [
              '#{node.resultVar} = (function(#{(options.trackLineAndColumn ? ["offset", "line", "column"] : ["offset"]).concat(keys(node.params)).join(", ")}) {#{node.code}})(#{(options.trackLineAndColumn ? ["pos.offset", "pos.line", "pos.column"] : ["pos"]).concat(values(node.params)).join(", ")}) ? "" : null;'
            ],
            semantic_not: [
              '#{node.resultVar} = (function(#{(options.trackLineAndColumn ? ["offset", "line", "column"] : ["offset"]).concat(keys(node.params)).join(", ")}) {#{node.code}})(#{(options.trackLineAndColumn ? ["pos.offset", "pos.line", "pos.column"] : ["pos"]).concat(values(node.params)).join(", ")}) ? null : "";'
            ],
            optional: [
              "#block emit(node.expression)",
              '#{node.resultVar} = #{node.resultVar} !== null ? #{node.resultVar} : "";'
            ],
            zero_or_more: [
              "#{node.resultVar} = [];",
              "#block emit(node.expression)",
              "while (#{node.expression.resultVar} !== null) {",
              "  #{node.resultVar}.push(#{node.expression.resultVar});",
              "  #block emit(node.expression)",
              "}"
            ],
            one_or_more: [
              "#block emit(node.expression)",
              "if (#{node.expression.resultVar} !== null) {",
              "  #{node.resultVar} = [];",
              "  while (#{node.expression.resultVar} !== null) {",
              "    #{node.resultVar}.push(#{node.expression.resultVar});",
              "    #block emit(node.expression)",
              "  }",
              "} else {",
              "  #{node.resultVar} = null;",
              "}"
            ],
            action: [
              "#{posSave(node)};",
              "#block emit(node.expression)",
              "if (#{node.resultVar} !== null) {",
              '  #{node.resultVar} = (function(#{(options.trackLineAndColumn ? ["offset", "line", "column"] : ["offset"]).concat(keys(node.params)).join(", ")}) {#{node.code}})(#{(options.trackLineAndColumn ? [node.posVar + ".offset", node.posVar + ".line", node.posVar + ".column"] : [node.posVar]).concat(values(node.params)).join(", ")});',
              "}",
              "if (#{node.resultVar} === null) {",
              "  #{posRestore(node)};",
              "}"
            ],
            rule_ref: [
              "#{node.resultVar} = parse_#{node.name}();"
            ],
            literal: [
              "#if node.value.length === 0",
              '  #{node.resultVar} = "";',
              "#else",
              "  #if !node.ignoreCase",
              "    #if node.value.length === 1",
              '      if (input.charCodeAt(#{posOffset("pos")}) === #{node.value.charCodeAt(0)}) {',
              "    #else",
              '      if (input.substr(#{posOffset("pos")}, #{node.value.length}) === #{string(node.value)}) {',
              "    #end",
              "  #else",
              /*
               * One-char literals are not optimized when case-insensitive
               * matching is enabled. This is because there is no simple way to
               * lowercase a character code that works for character outside ASCII
               * letters. Moreover, |toLowerCase| can change string length,
               * meaning the result of lowercasing a character can be more
               * characters.
               */
              '    if (input.substr(#{posOffset("pos")}, #{node.value.length}).toLowerCase() === #{string(node.value.toLowerCase())}) {',
              "  #end",
              "    #if !node.ignoreCase",
              "      #{node.resultVar} = #{string(node.value)};",
              "    #else",
              '      #{node.resultVar} = input.substr(#{posOffset("pos")}, #{node.value.length});',
              "    #end",
              "    #{posAdvance(node.value.length)};",
              "  } else {",
              "    #{node.resultVar} = null;",
              "    if (reportFailures === 0) {",
              "      matchFailed(#{string(string(node.value))});",
              "    }",
              "  }",
              "#end"
            ],
            any: [
              'if (input.length > #{posOffset("pos")}) {',
              '  #{node.resultVar} = input.charAt(#{posOffset("pos")});',
              "  #{posAdvance(1)};",
              "} else {",
              "  #{node.resultVar} = null;",
              "  if (reportFailures === 0) {",
              '    matchFailed("any character");',
              "  }",
              "}"
            ],
            "class": [
              'if (#{regexp}.test(input.charAt(#{posOffset("pos")}))) {',
              '  #{node.resultVar} = input.charAt(#{posOffset("pos")});',
              "  #{posAdvance(1)};",
              "} else {",
              "  #{node.resultVar} = null;",
              "  if (reportFailures === 0) {",
              "    matchFailed(#{string(node.rawText)});",
              "  }",
              "}"
            ]
          };
          for (name in sources) {
            templates2[name] = Codie.template(sources[name].join("\n"));
          }
          return templates2;
        })();
        function fill(name, vars) {
          vars.string = quote;
          vars.pluck = pluck;
          vars.keys = keys;
          vars.values = values;
          vars.emit = emit;
          vars.options = options2;
          if (options2.trackLineAndColumn) {
            vars.posInit = function(name2) {
              return "var " + name2 + " = { offset: 0, line: 1, column: 1, seenCR: false }";
            };
            vars.posClone = function(name2) {
              return "clone(" + name2 + ")";
            };
            vars.posOffset = function(name2) {
              return name2 + ".offset";
            };
            vars.posAdvance = function(n) {
              return "advance(pos, " + n + ")";
            };
          } else {
            vars.posInit = function(name2) {
              return "var " + name2 + " = 0";
            };
            vars.posClone = function(name2) {
              return name2;
            };
            vars.posOffset = function(name2) {
              return name2;
            };
            vars.posAdvance = function(n) {
              return n === 1 ? "pos++" : "pos += " + n;
            };
          }
          vars.posSave = function(node) {
            return node.posVar + " = " + vars.posClone("pos");
          };
          vars.posRestore = function(node) {
            return "pos = " + vars.posClone(node.posVar);
          };
          return templates[name](vars);
        }
        function emitSimple(name) {
          return function(node) {
            return fill(name, { node });
          };
        }
        var emit = buildNodeVisitor({
          grammar: emitSimple("grammar"),
          initializer: function(node) {
            return node.code;
          },
          rule: emitSimple("rule"),
          /*
           * The contract for all code fragments generated by the following functions
           * is as follows.
           *
           * The code fragment tries to match a part of the input starting with the
           * position indicated in |pos|. That position may point past the end of the
           * input.
           *
           * * If the code fragment matches the input, it advances |pos| to point to
           *   the first chracter following the matched part of the input and sets
           *   variable with a name stored in |node.resultVar| to an appropriate
           *   value. This value is always non-|null|.
           *
           * * If the code fragment does not match the input, it returns with |pos|
           *   set to the original value and it sets a variable with a name stored in
           *   |node.resultVar| to |null|.
           *
           * The code can use variables with names stored in |resultVar| and |posVar|
           * properties of the current node's subnodes. It can't use any other
           * variables.
           */
          choice: function(node) {
            var code, nextAlternativesCode;
            for (var i = node.alternatives.length - 1; i >= 0; i--) {
              nextAlternativesCode = i !== node.alternatives.length - 1 ? fill("choice.next", { node, code }) : "";
              code = fill("choice", {
                alternative: node.alternatives[i],
                nextAlternativesCode
              });
            }
            return code;
          },
          sequence: function(node) {
            var code = fill("sequence.inner", { node });
            for (var i = node.elements.length - 1; i >= 0; i--) {
              code = fill("sequence.iteration", {
                node,
                element: node.elements[i],
                code
              });
            }
            return fill("sequence", { node, code });
          },
          labeled: function(node) {
            return emit(node.expression);
          },
          simple_and: emitSimple("simple_and"),
          simple_not: emitSimple("simple_not"),
          semantic_and: emitSimple("semantic_and"),
          semantic_not: emitSimple("semantic_not"),
          optional: emitSimple("optional"),
          zero_or_more: emitSimple("zero_or_more"),
          one_or_more: emitSimple("one_or_more"),
          action: emitSimple("action"),
          rule_ref: emitSimple("rule_ref"),
          literal: emitSimple("literal"),
          any: emitSimple("any"),
          "class": function(node) {
            var regexp;
            if (node.parts.length > 0) {
              regexp = "/^[" + (node.inverted ? "^" : "") + map(node.parts, function(part) {
                return part instanceof Array ? quoteForRegexpClass(part[0]) + "-" + quoteForRegexpClass(part[1]) : quoteForRegexpClass(part);
              }).join("") + "]/" + (node.ignoreCase ? "i" : "");
            } else {
              regexp = node.inverted ? "/^[\\S\\s]/" : "/^(?!)/";
            }
            return fill("class", { node, regexp });
          }
        });
        return emit(ast2);
      };
      return PEG;
    })();
    if (typeof module !== "undefined") {
      module.exports = PEG;
    }
  }
});

// ../../../../node_modules/quickpeg/index.js
var require_quickpeg = __commonJS({
  "../../../../node_modules/quickpeg/index.js"(exports2, module2) {
    var pegjs = require_peg();
    var fsCompare = require_fs_compare();
    var fs = require_fs();
    var Path = require_path();
    var parseFile = function(parser, file, cb) {
      fs.readFile(file, "utf-8", function(err, source2) {
        if (err) {
          return cb(err);
        }
        try {
          var result2 = parser.parse(source2);
          return cb(null, result2);
        } catch (e) {
          return cb(e);
        }
      });
    };
    var saveParser = function(source2, parserFile, cb) {
      var moduleSource = "module.exports = " + source2;
      fs.writeFile(parserFile, moduleSource, function(err) {
        if (err) {
          return cb(err);
        }
        cb(null);
      });
    };
    var extendParser = function(parser) {
      parser.parseFile = parseFile.bind(null, parser);
    };
    var loadParser = function(grammarFile, options2, cb) {
      fs.readFile(grammarFile, "utf8", function(err, grammarSource) {
        if (err) {
          return cb(err);
        }
        try {
          var parser = pegjs.buildParser(grammarSource, options2.pegjs || {});
          extendParser(parser);
          return cb(null, parser);
        } catch (e) {
          return cb(e);
        }
      });
    };
    var loadAndSaveParser = function(grammarFile, parserFile, options2, cb) {
      loadParser(grammarFile, options2, function(err, parser) {
        if (err) {
          return cb(err);
        }
        saveParser(parser.toSource(), parserFile, function(err2) {
          if (err2) {
            return cb(err2);
          }
          return cb(null, parser);
        });
      });
    };
    var cachedParserFromFile = function(grammarFile, parserFile, options2, cb) {
      fsCompare.mtime(grammarFile, parserFile, function(err, diff) {
        var parser;
        if (err) {
          return cb(err);
        }
        if (diff > 0) {
          return loadAndSaveParser(grammarFile, parserFile, options2, cb);
        } else {
          try {
            parser = __require(parserFile);
            extendParser(parser);
          } catch (e) {
            return cb(e);
          }
          return cb(null, parser);
        }
      });
    };
    var cachedParserFile = function(grammarFile, cache, cb) {
      var parserFilename = Path.basename(grammarFile + ".js");
      if (typeof cache === "string") {
        fs.stat(cache, function(err, stat) {
          if (err) {
            fs.stat(Path.dirname(cache), function(err2, stat2) {
              if (err2) {
                return cb(err2);
              }
              return cb(null, cache);
            });
          } else {
            if (stat.isDirectory()) {
              return cb(null, Path.join(cache, parserFilename));
            } else {
              return cb(null, cache);
            }
          }
        });
      } else {
        return cb(null, grammarFile + ".js");
      }
    };
    var parserCache = [];
    var parserFromFile = function(defaultOptions, grammarFile, extraOptions, cb) {
      var options2 = {};
      Object.keys(defaultOptions).forEach(function(key) {
        options2[key] = defaultOptions[key];
      });
      if (typeof cb === "undefined") {
        cb = extraOptions;
        extraOptions = {};
      }
      Object.keys(extraOptions).forEach(function(key) {
        options2[key] = extraOptions[key];
      });
      if (options2.cache) {
        if (grammarFile in parserCache) {
          return cb(null, parserCache[grammarFile]);
        }
        cachedParserFile(grammarFile, options2.cache, function(err, parserFile) {
          if (err) {
            return cb(err);
          }
          cachedParserFromFile(grammarFile, parserFile, options2, function(err2, parser) {
            if (err2) {
              return cb(err2);
            }
            parserCache[grammarFile] = parser;
            return cb(null, parser);
          });
        });
      } else {
        loadParser(grammarFile, options2, cb);
      }
    };
    var parserFromMemory = function(grammarFile) {
      return grammarFile in parserCache ? parserCache[grammarFile] : null;
    };
    var flushMemory = function() {
      parserCache = [];
    };
    var config = function(options2) {
      options2 = options2 || {};
      if (typeof options2.cache === "undefined") {
        options2.cache = true;
      }
      var fn = parserFromFile.bind(null, options2);
      fn.config = config;
      fn.flushMemory = flushMemory;
      fn.parserFromMemory = parserFromMemory;
      return fn;
    };
    module2.exports = config();
  }
});

// browser-external:util
var require_util = __commonJS({
  "browser-external:util"(exports2, module2) {
    module2.exports = Object.create(new Proxy({}, {
      get(_, key) {
        if (key !== "__esModule" && key !== "__proto__" && key !== "constructor" && key !== "splice") {
          console.warn(`Module "util" has been externalized for browser compatibility. Cannot access "util.${key}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
        }
      }
    }));
  }
});

// ../../../../node_modules/doctor/lib/ast-node.js
var require_ast_node = __commonJS({
  "../../../../node_modules/doctor/lib/ast-node.js"(exports2, module2) {
    var nast = require_nice_ast();
    var util = require_util();
    function AstNode(node, parent) {
      this.parent = parent || null;
      this.prev = null;
      this.next = null;
      this._items = {};
      var self = this;
      Object.keys(node).forEach(function(key, i) {
        self[key] = node[key];
      });
    }
    function getNodeItem(key) {
      if (key in this._items) {
        return this._items[key];
      }
      if (this.parent) {
        return getNodeItem.call(this.parent, key);
      }
      return null;
    }
    AstNode.prototype.item = function(key, value) {
      if (typeof value === "undefined") {
        return getNodeItem.call(this, key);
      }
      this._items[key] = value;
    };
    AstNode.prototype.fix = function(index) {
      var nodes = this.parent.nodes;
      if (index > 0) {
        var prev = nodes[index - 1];
        prev.next = this;
        this.prev = prev;
      } else {
        this.prev = null;
      }
      if (index < nodes.length - 1) {
        var next = nodes[index + 1];
        next.prev = this;
        this.next = next;
      } else {
        this.next = null;
      }
    };
    AstNode.prototype.index = function() {
      if (!this.parent || !this.parent.nodes) {
        return 0;
      }
      var nodes = this.parent.nodes;
      for (var i = 0; i < nodes.length; i++) {
        var child = nodes[i];
        if (child === this) {
          return i;
        }
      }
      return 0;
    };
    AstNode.prototype.walk = function(node) {
      if (this._walk) {
        this._walk(node);
      }
    };
    AstNode.prototype.after = function(node) {
      node = nast.extendAst(node);
      node.parent = this.parent;
      var nodes = this.parent.nodes;
      var index = this.index();
      nodes.splice(index + 1, 0, node);
      node.fix(index + 1);
      this.walk(node);
    };
    AstNode.prototype.before = function(node) {
      if (this.prev) {
        this.prev.after(node);
      } else {
        this.parent.prepend(node);
      }
    };
    AstNode.prototype.prepend = function(node) {
      node = nast.extendAst(node);
      this.nodes.unshift(node);
      node.parent = this;
      node.fix(0);
      this.walk(node);
    };
    AstNode.prototype.append = function(node) {
      node = nast.extendAst(node);
      this.nodes.push(node);
      node.parent = this;
      node.fix(this.nodes.length - 1);
      this.walk(node);
    };
    AstNode.prototype.remove = function() {
      this._removed = true;
      var index = this.index();
      this.parent.nodes.splice(index, 1);
      if (this.prev) {
        this.prev.fix(index - 1);
      }
      if (this.next) {
        this.next.fix(index);
      }
    };
    AstNode.prototype.lispify = function() {
      return nast.lispify(this);
    };
    AstNode.prototype.like = function(node) {
      return nast.like(this, node);
    };
    AstNode.prototype.equal = function(node) {
      return nast.equal(this, node);
    };
    AstNode.prototype.parser = function() {
      var parser = null;
      if (this.grammarFile) {
        parser = nast.parserForGrammar(this.grammarFile);
      } else if (this.parent) {
        return this.parent.parser();
      }
      return parser;
    };
    AstNode.prototype.likeSource = function(source2) {
      var parser = this.parser();
      if (!parser) {
        return false;
      }
      var ast2 = parser.parse(source2);
      var nodes = ast2.nodes;
      if (nodes.length !== 1) {
        return false;
      }
      return this.like(nodes[0]);
    };
    AstNode.prototype.ast = function() {
      return nast.cleanAst(this);
    };
    AstNode.prototype.fromSource = function(source2) {
      var parser = this.parser();
      var ast2 = parser.parse(source2);
      var node = ast2.nodes[0];
      nast.walk(node, function(node2) {
        delete node2.pos;
        delete node2.comments;
      });
      return node;
    };
    AstNode.prototype.nodeFromSource = function(source2) {
      var node = this.fromSource(source2);
      return nast.extendAst(node);
    };
    module2.exports = AstNode;
  }
});

// ../../../../node_modules/underscore/underscore.js
var require_underscore = __commonJS({
  "../../../../node_modules/underscore/underscore.js"(exports2, module2) {
    (function() {
      var root = this;
      var previousUnderscore = root._;
      var breaker = {};
      var ArrayProto = Array.prototype, ObjProto = Object.prototype, FuncProto = Function.prototype;
      var slice = ArrayProto.slice, unshift = ArrayProto.unshift, toString = ObjProto.toString, hasOwnProperty = ObjProto.hasOwnProperty;
      var nativeForEach = ArrayProto.forEach, nativeMap = ArrayProto.map, nativeReduce = ArrayProto.reduce, nativeReduceRight = ArrayProto.reduceRight, nativeFilter = ArrayProto.filter, nativeEvery = ArrayProto.every, nativeSome = ArrayProto.some, nativeIndexOf = ArrayProto.indexOf, nativeLastIndexOf = ArrayProto.lastIndexOf, nativeIsArray = Array.isArray, nativeKeys = Object.keys, nativeBind = FuncProto.bind;
      var _ = function(obj) {
        return new wrapper(obj);
      };
      if (typeof exports2 !== "undefined") {
        if (typeof module2 !== "undefined" && module2.exports) {
          exports2 = module2.exports = _;
        }
        exports2._ = _;
      } else if (typeof define === "function" && define.amd) {
        define("underscore", function() {
          return _;
        });
      } else {
        root["_"] = _;
      }
      _.VERSION = "1.2.4";
      var each2 = _.each = _.forEach = function(obj, iterator, context) {
        if (obj == null) return;
        if (nativeForEach && obj.forEach === nativeForEach) {
          obj.forEach(iterator, context);
        } else if (obj.length === +obj.length) {
          for (var i = 0, l = obj.length; i < l; i++) {
            if (i in obj && iterator.call(context, obj[i], i, obj) === breaker) return;
          }
        } else {
          for (var key in obj) {
            if (hasOwnProperty.call(obj, key)) {
              if (iterator.call(context, obj[key], key, obj) === breaker) return;
            }
          }
        }
      };
      _.map = function(obj, iterator, context) {
        var results = [];
        if (obj == null) return results;
        if (nativeMap && obj.map === nativeMap) return obj.map(iterator, context);
        each2(obj, function(value, index, list) {
          results[results.length] = iterator.call(context, value, index, list);
        });
        if (obj.length === +obj.length) results.length = obj.length;
        return results;
      };
      _.reduce = _.foldl = _.inject = function(obj, iterator, memo, context) {
        var initial = arguments.length > 2;
        if (obj == null) obj = [];
        if (nativeReduce && obj.reduce === nativeReduce) {
          if (context) iterator = _.bind(iterator, context);
          return initial ? obj.reduce(iterator, memo) : obj.reduce(iterator);
        }
        each2(obj, function(value, index, list) {
          if (!initial) {
            memo = value;
            initial = true;
          } else {
            memo = iterator.call(context, memo, value, index, list);
          }
        });
        if (!initial) throw new TypeError("Reduce of empty array with no initial value");
        return memo;
      };
      _.reduceRight = _.foldr = function(obj, iterator, memo, context) {
        var initial = arguments.length > 2;
        if (obj == null) obj = [];
        if (nativeReduceRight && obj.reduceRight === nativeReduceRight) {
          if (context) iterator = _.bind(iterator, context);
          return initial ? obj.reduceRight(iterator, memo) : obj.reduceRight(iterator);
        }
        var reversed = _.toArray(obj).reverse();
        if (context && !initial) iterator = _.bind(iterator, context);
        return initial ? _.reduce(reversed, iterator, memo, context) : _.reduce(reversed, iterator);
      };
      _.find = _.detect = function(obj, iterator, context) {
        var result3;
        any(obj, function(value, index, list) {
          if (iterator.call(context, value, index, list)) {
            result3 = value;
            return true;
          }
        });
        return result3;
      };
      _.filter = _.select = function(obj, iterator, context) {
        var results = [];
        if (obj == null) return results;
        if (nativeFilter && obj.filter === nativeFilter) return obj.filter(iterator, context);
        each2(obj, function(value, index, list) {
          if (iterator.call(context, value, index, list)) results[results.length] = value;
        });
        return results;
      };
      _.reject = function(obj, iterator, context) {
        var results = [];
        if (obj == null) return results;
        each2(obj, function(value, index, list) {
          if (!iterator.call(context, value, index, list)) results[results.length] = value;
        });
        return results;
      };
      _.every = _.all = function(obj, iterator, context) {
        var result3 = true;
        if (obj == null) return result3;
        if (nativeEvery && obj.every === nativeEvery) return obj.every(iterator, context);
        each2(obj, function(value, index, list) {
          if (!(result3 = result3 && iterator.call(context, value, index, list))) return breaker;
        });
        return result3;
      };
      var any = _.some = _.any = function(obj, iterator, context) {
        iterator || (iterator = _.identity);
        var result3 = false;
        if (obj == null) return result3;
        if (nativeSome && obj.some === nativeSome) return obj.some(iterator, context);
        each2(obj, function(value, index, list) {
          if (result3 || (result3 = iterator.call(context, value, index, list))) return breaker;
        });
        return !!result3;
      };
      _.include = _.contains = function(obj, target) {
        var found = false;
        if (obj == null) return found;
        if (nativeIndexOf && obj.indexOf === nativeIndexOf) return obj.indexOf(target) != -1;
        found = any(obj, function(value) {
          return value === target;
        });
        return found;
      };
      _.invoke = function(obj, method) {
        var args = slice.call(arguments, 2);
        return _.map(obj, function(value) {
          return (_.isFunction(method) ? method || value : value[method]).apply(value, args);
        });
      };
      _.pluck = function(obj, key) {
        return _.map(obj, function(value) {
          return value[key];
        });
      };
      _.max = function(obj, iterator, context) {
        if (!iterator && _.isArray(obj)) return Math.max.apply(Math, obj);
        if (!iterator && _.isEmpty(obj)) return -Infinity;
        var result3 = { computed: -Infinity };
        each2(obj, function(value, index, list) {
          var computed = iterator ? iterator.call(context, value, index, list) : value;
          computed >= result3.computed && (result3 = { value, computed });
        });
        return result3.value;
      };
      _.min = function(obj, iterator, context) {
        if (!iterator && _.isArray(obj)) return Math.min.apply(Math, obj);
        if (!iterator && _.isEmpty(obj)) return Infinity;
        var result3 = { computed: Infinity };
        each2(obj, function(value, index, list) {
          var computed = iterator ? iterator.call(context, value, index, list) : value;
          computed < result3.computed && (result3 = { value, computed });
        });
        return result3.value;
      };
      _.shuffle = function(obj) {
        var shuffled = [], rand;
        each2(obj, function(value, index, list) {
          if (index == 0) {
            shuffled[0] = value;
          } else {
            rand = Math.floor(Math.random() * (index + 1));
            shuffled[index] = shuffled[rand];
            shuffled[rand] = value;
          }
        });
        return shuffled;
      };
      _.sortBy = function(obj, iterator, context) {
        return _.pluck(_.map(obj, function(value, index, list) {
          return {
            value,
            criteria: iterator.call(context, value, index, list)
          };
        }).sort(function(left, right) {
          var a = left.criteria, b = right.criteria;
          return a < b ? -1 : a > b ? 1 : 0;
        }), "value");
      };
      _.groupBy = function(obj, val) {
        var result3 = {};
        var iterator = _.isFunction(val) ? val : function(obj2) {
          return obj2[val];
        };
        each2(obj, function(value, index) {
          var key = iterator(value, index);
          (result3[key] || (result3[key] = [])).push(value);
        });
        return result3;
      };
      _.sortedIndex = function(array, obj, iterator) {
        iterator || (iterator = _.identity);
        var low = 0, high = array.length;
        while (low < high) {
          var mid = low + high >> 1;
          iterator(array[mid]) < iterator(obj) ? low = mid + 1 : high = mid;
        }
        return low;
      };
      _.toArray = function(iterable) {
        if (!iterable) return [];
        if (iterable.toArray) return iterable.toArray();
        if (_.isArray(iterable)) return slice.call(iterable);
        if (_.isArguments(iterable)) return slice.call(iterable);
        return _.values(iterable);
      };
      _.size = function(obj) {
        return _.toArray(obj).length;
      };
      _.first = _.head = function(array, n, guard) {
        return n != null && !guard ? slice.call(array, 0, n) : array[0];
      };
      _.initial = function(array, n, guard) {
        return slice.call(array, 0, array.length - (n == null || guard ? 1 : n));
      };
      _.last = function(array, n, guard) {
        if (n != null && !guard) {
          return slice.call(array, Math.max(array.length - n, 0));
        } else {
          return array[array.length - 1];
        }
      };
      _.rest = _.tail = function(array, index, guard) {
        return slice.call(array, index == null || guard ? 1 : index);
      };
      _.compact = function(array) {
        return _.filter(array, function(value) {
          return !!value;
        });
      };
      _.flatten = function(array, shallow) {
        return _.reduce(array, function(memo, value) {
          if (_.isArray(value)) return memo.concat(shallow ? value : _.flatten(value));
          memo[memo.length] = value;
          return memo;
        }, []);
      };
      _.without = function(array) {
        return _.difference(array, slice.call(arguments, 1));
      };
      _.uniq = _.unique = function(array, isSorted, iterator) {
        var initial = iterator ? _.map(array, iterator) : array;
        var result3 = [];
        _.reduce(initial, function(memo, el, i) {
          if (0 == i || (isSorted === true ? _.last(memo) != el : !_.include(memo, el))) {
            memo[memo.length] = el;
            result3[result3.length] = array[i];
          }
          return memo;
        }, []);
        return result3;
      };
      _.union = function() {
        return _.uniq(_.flatten(arguments, true));
      };
      _.intersection = _.intersect = function(array) {
        var rest = slice.call(arguments, 1);
        return _.filter(_.uniq(array), function(item) {
          return _.every(rest, function(other) {
            return _.indexOf(other, item) >= 0;
          });
        });
      };
      _.difference = function(array) {
        var rest = _.flatten(slice.call(arguments, 1));
        return _.filter(array, function(value) {
          return !_.include(rest, value);
        });
      };
      _.zip = function() {
        var args = slice.call(arguments);
        var length = _.max(_.pluck(args, "length"));
        var results = new Array(length);
        for (var i = 0; i < length; i++) results[i] = _.pluck(args, "" + i);
        return results;
      };
      _.indexOf = function(array, item, isSorted) {
        if (array == null) return -1;
        var i, l;
        if (isSorted) {
          i = _.sortedIndex(array, item);
          return array[i] === item ? i : -1;
        }
        if (nativeIndexOf && array.indexOf === nativeIndexOf) return array.indexOf(item);
        for (i = 0, l = array.length; i < l; i++) if (i in array && array[i] === item) return i;
        return -1;
      };
      _.lastIndexOf = function(array, item) {
        if (array == null) return -1;
        if (nativeLastIndexOf && array.lastIndexOf === nativeLastIndexOf) return array.lastIndexOf(item);
        var i = array.length;
        while (i--) if (i in array && array[i] === item) return i;
        return -1;
      };
      _.range = function(start, stop, step) {
        if (arguments.length <= 1) {
          stop = start || 0;
          start = 0;
        }
        step = arguments[2] || 1;
        var len = Math.max(Math.ceil((stop - start) / step), 0);
        var idx = 0;
        var range2 = new Array(len);
        while (idx < len) {
          range2[idx++] = start;
          start += step;
        }
        return range2;
      };
      var ctor = function() {
      };
      _.bind = function bind(func, context) {
        var bound, args;
        if (func.bind === nativeBind && nativeBind) return nativeBind.apply(func, slice.call(arguments, 1));
        if (!_.isFunction(func)) throw new TypeError();
        args = slice.call(arguments, 2);
        return bound = function() {
          if (!(this instanceof bound)) return func.apply(context, args.concat(slice.call(arguments)));
          ctor.prototype = func.prototype;
          var self = new ctor();
          var result3 = func.apply(self, args.concat(slice.call(arguments)));
          if (Object(result3) === result3) return result3;
          return self;
        };
      };
      _.bindAll = function(obj) {
        var funcs = slice.call(arguments, 1);
        if (funcs.length == 0) funcs = _.functions(obj);
        each2(funcs, function(f) {
          obj[f] = _.bind(obj[f], obj);
        });
        return obj;
      };
      _.memoize = function(func, hasher) {
        var memo = {};
        hasher || (hasher = _.identity);
        return function() {
          var key = hasher.apply(this, arguments);
          return hasOwnProperty.call(memo, key) ? memo[key] : memo[key] = func.apply(this, arguments);
        };
      };
      _.delay = function(func, wait) {
        var args = slice.call(arguments, 2);
        return setTimeout(function() {
          return func.apply(func, args);
        }, wait);
      };
      _.defer = function(func) {
        return _.delay.apply(_, [func, 1].concat(slice.call(arguments, 1)));
      };
      _.throttle = function(func, wait) {
        var context, args, timeout, throttling, more;
        var whenDone = _.debounce(function() {
          more = throttling = false;
        }, wait);
        return function() {
          context = this;
          args = arguments;
          var later = function() {
            timeout = null;
            if (more) func.apply(context, args);
            whenDone();
          };
          if (!timeout) timeout = setTimeout(later, wait);
          if (throttling) {
            more = true;
          } else {
            func.apply(context, args);
          }
          whenDone();
          throttling = true;
        };
      };
      _.debounce = function(func, wait) {
        var timeout;
        return function() {
          var context = this, args = arguments;
          var later = function() {
            timeout = null;
            func.apply(context, args);
          };
          clearTimeout(timeout);
          timeout = setTimeout(later, wait);
        };
      };
      _.once = function(func) {
        var ran = false, memo;
        return function() {
          if (ran) return memo;
          ran = true;
          return memo = func.apply(this, arguments);
        };
      };
      _.wrap = function(func, wrapper2) {
        return function() {
          var args = [func].concat(slice.call(arguments, 0));
          return wrapper2.apply(this, args);
        };
      };
      _.compose = function() {
        var funcs = arguments;
        return function() {
          var args = arguments;
          for (var i = funcs.length - 1; i >= 0; i--) {
            args = [funcs[i].apply(this, args)];
          }
          return args[0];
        };
      };
      _.after = function(times, func) {
        if (times <= 0) return func();
        return function() {
          if (--times < 1) {
            return func.apply(this, arguments);
          }
        };
      };
      _.keys = nativeKeys || function(obj) {
        if (obj !== Object(obj)) throw new TypeError("Invalid object");
        var keys2 = [];
        for (var key in obj) if (hasOwnProperty.call(obj, key)) keys2[keys2.length] = key;
        return keys2;
      };
      _.values = function(obj) {
        return _.map(obj, _.identity);
      };
      _.functions = _.methods = function(obj) {
        var names = [];
        for (var key in obj) {
          if (_.isFunction(obj[key])) names.push(key);
        }
        return names.sort();
      };
      _.extend = function(obj) {
        each2(slice.call(arguments, 1), function(source2) {
          for (var prop in source2) {
            if (source2[prop] !== void 0) obj[prop] = source2[prop];
          }
        });
        return obj;
      };
      _.defaults = function(obj) {
        each2(slice.call(arguments, 1), function(source2) {
          for (var prop in source2) {
            if (obj[prop] == null) obj[prop] = source2[prop];
          }
        });
        return obj;
      };
      _.clone = function(obj) {
        if (!_.isObject(obj)) return obj;
        return _.isArray(obj) ? obj.slice() : _.extend({}, obj);
      };
      _.tap = function(obj, interceptor) {
        interceptor(obj);
        return obj;
      };
      function eq(a, b, stack) {
        if (a === b) return a !== 0 || 1 / a == 1 / b;
        if (a == null || b == null) return a === b;
        if (a._chain) a = a._wrapped;
        if (b._chain) b = b._wrapped;
        if (a.isEqual && _.isFunction(a.isEqual)) return a.isEqual(b);
        if (b.isEqual && _.isFunction(b.isEqual)) return b.isEqual(a);
        var className = toString.call(a);
        if (className != toString.call(b)) return false;
        switch (className) {
          // Strings, numbers, dates, and booleans are compared by value.
          case "[object String]":
            return a == String(b);
          case "[object Number]":
            return a != +a ? b != +b : a == 0 ? 1 / a == 1 / b : a == +b;
          case "[object Date]":
          case "[object Boolean]":
            return +a == +b;
          // RegExps are compared by their source patterns and flags.
          case "[object RegExp]":
            return a.source == b.source && a.global == b.global && a.multiline == b.multiline && a.ignoreCase == b.ignoreCase;
        }
        if (typeof a != "object" || typeof b != "object") return false;
        var length = stack.length;
        while (length--) {
          if (stack[length] == a) return true;
        }
        stack.push(a);
        var size = 0, result3 = true;
        if (className == "[object Array]") {
          size = a.length;
          result3 = size == b.length;
          if (result3) {
            while (size--) {
              if (!(result3 = size in a == size in b && eq(a[size], b[size], stack))) break;
            }
          }
        } else {
          if ("constructor" in a != "constructor" in b || a.constructor != b.constructor) return false;
          for (var key in a) {
            if (hasOwnProperty.call(a, key)) {
              size++;
              if (!(result3 = hasOwnProperty.call(b, key) && eq(a[key], b[key], stack))) break;
            }
          }
          if (result3) {
            for (key in b) {
              if (hasOwnProperty.call(b, key) && !size--) break;
            }
            result3 = !size;
          }
        }
        stack.pop();
        return result3;
      }
      _.isEqual = function(a, b) {
        return eq(a, b, []);
      };
      _.isEmpty = function(obj) {
        if (_.isArray(obj) || _.isString(obj)) return obj.length === 0;
        for (var key in obj) if (hasOwnProperty.call(obj, key)) return false;
        return true;
      };
      _.isElement = function(obj) {
        return !!(obj && obj.nodeType == 1);
      };
      _.isArray = nativeIsArray || function(obj) {
        return toString.call(obj) == "[object Array]";
      };
      _.isObject = function(obj) {
        return obj === Object(obj);
      };
      _.isArguments = function(obj) {
        return toString.call(obj) == "[object Arguments]";
      };
      if (!_.isArguments(arguments)) {
        _.isArguments = function(obj) {
          return !!(obj && hasOwnProperty.call(obj, "callee"));
        };
      }
      _.isFunction = function(obj) {
        return toString.call(obj) == "[object Function]";
      };
      _.isString = function(obj) {
        return toString.call(obj) == "[object String]";
      };
      _.isNumber = function(obj) {
        return toString.call(obj) == "[object Number]";
      };
      _.isNaN = function(obj) {
        return obj !== obj;
      };
      _.isBoolean = function(obj) {
        return obj === true || obj === false || toString.call(obj) == "[object Boolean]";
      };
      _.isDate = function(obj) {
        return toString.call(obj) == "[object Date]";
      };
      _.isRegExp = function(obj) {
        return toString.call(obj) == "[object RegExp]";
      };
      _.isNull = function(obj) {
        return obj === null;
      };
      _.isUndefined = function(obj) {
        return obj === void 0;
      };
      _.noConflict = function() {
        root._ = previousUnderscore;
        return this;
      };
      _.identity = function(value) {
        return value;
      };
      _.times = function(n, iterator, context) {
        for (var i = 0; i < n; i++) iterator.call(context, i);
      };
      _.escape = function(string) {
        return ("" + string).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#x27;").replace(/\//g, "&#x2F;");
      };
      _.mixin = function(obj) {
        each2(_.functions(obj), function(name) {
          addToWrapper(name, _[name] = obj[name]);
        });
      };
      var idCounter = 0;
      _.uniqueId = function(prefix) {
        var id = idCounter++;
        return prefix ? prefix + id : id;
      };
      _.templateSettings = {
        evaluate: /<%([\s\S]+?)%>/g,
        interpolate: /<%=([\s\S]+?)%>/g,
        escape: /<%-([\s\S]+?)%>/g
      };
      var noMatch = /.^/;
      _.template = function(str, data) {
        var c = _.templateSettings;
        var tmpl = "var __p=[],print=function(){__p.push.apply(__p,arguments);};with(obj||{}){__p.push('" + str.replace(/\\/g, "\\\\").replace(/'/g, "\\'").replace(c.escape || noMatch, function(match, code) {
          return "',_.escape(" + code.replace(/\\'/g, "'") + "),'";
        }).replace(c.interpolate || noMatch, function(match, code) {
          return "'," + code.replace(/\\'/g, "'") + ",'";
        }).replace(c.evaluate || noMatch, function(match, code) {
          return "');" + code.replace(/\\'/g, "'").replace(/[\r\n\t]/g, " ").replace(/\\\\/g, "\\") + ";__p.push('";
        }).replace(/\r/g, "\\r").replace(/\n/g, "\\n").replace(/\t/g, "\\t") + "');}return __p.join('');";
        var func = new Function("obj", "_", tmpl);
        if (data) return func(data, _);
        return function(data2) {
          return func.call(this, data2, _);
        };
      };
      _.chain = function(obj) {
        return _(obj).chain();
      };
      var wrapper = function(obj) {
        this._wrapped = obj;
      };
      _.prototype = wrapper.prototype;
      var result2 = function(obj, chain) {
        return chain ? _(obj).chain() : obj;
      };
      var addToWrapper = function(name, func) {
        wrapper.prototype[name] = function() {
          var args = slice.call(arguments);
          unshift.call(args, this._wrapped);
          return result2(func.apply(_, args), this._chain);
        };
      };
      _.mixin(_);
      each2(["pop", "push", "reverse", "shift", "sort", "splice", "unshift"], function(name) {
        var method = ArrayProto[name];
        wrapper.prototype[name] = function() {
          var wrapped = this._wrapped;
          method.apply(wrapped, arguments);
          var length = wrapped.length;
          if ((name == "shift" || name == "splice") && length === 0) delete wrapped[0];
          return result2(wrapped, this._chain);
        };
      });
      each2(["concat", "join", "slice"], function(name) {
        var method = ArrayProto[name];
        wrapper.prototype[name] = function() {
          return result2(method.apply(this._wrapped, arguments), this._chain);
        };
      });
      wrapper.prototype.chain = function() {
        this._chain = true;
        return this;
      };
      wrapper.prototype.value = function() {
        return this._wrapped;
      };
    }).call(exports2);
  }
});

// ../../../../node_modules/doctor/lib/util.js
var require_util2 = __commonJS({
  "../../../../node_modules/doctor/lib/util.js"(exports2, module2) {
    var fs = require_fs();
    var path = require_path();
    var async = require_async();
    var _ = require_underscore();
    function toArray(maybeArray) {
      if (Array.isArray(maybeArray)) {
        return maybeArray;
      }
      if (typeof maybeArray === "undefined") {
        return [];
      }
      return [maybeArray];
    }
    function requireMaybe(moduleName) {
      try {
        var modulePath = __require.resolve(moduleName);
      } catch (e) {
        return null;
      }
      return __require(moduleName);
    }
    function findRules(options2, optionRules, defaultDir) {
      options2.progressCb({
        type: "find-rules",
        message: "find rules for: " + defaultDir
      });
      var allRules = [];
      optionRules = toArray(optionRules);
      optionRules.forEach(function(rules, i) {
        if (Array.isArray(rules)) {
          allRules = allRules.concat(rules);
          return;
        }
        if (typeof rules !== "string") {
          throw new Error("rules must be a string or array");
        }
        var m;
        var rulesPath;
        if (!m && rules[0] === "/") {
          rulesPath = rules;
          m = requireMaybe(rules);
        }
        if (!m) {
          rulesPath = path.join(process.cwd(), rules);
          m = requireMaybe(rulesPath);
        }
        if (!m) {
          rulesPath = path.join("..", defaultDir, rules);
          m = requireMaybe(rulesPath);
        }
        if (!m) {
          throw new Error("did not find rules or problem loading rules: " + rules);
        } else {
          options2.progressCb({
            type: "found-rules",
            message: "found rules: " + rulesPath
          });
        }
        rules = m;
        if (m.rules) {
          rules = m.rules;
        }
        if (!Array.isArray(rules)) {
          throw new Error("rules module must contain an array");
        }
        allRules = allRules.concat(rules);
      });
      return allRules;
    }
    function findFunctions(optionFunctions, defaultDir) {
      var allFunctions = [];
      optionFunctions = toArray(optionFunctions);
      optionFunctions.forEach(function(fn, i) {
        if (typeof fn === "function") {
          allFunctions.push(fn);
          return;
        }
        if (typeof fn !== "string") {
          throw new Error("function must be a string or array");
        }
        var m = requireMaybe(fn);
        if (!m) {
          m = requireMaybe(path.join(process.cwd(), fn));
        }
        if (!m) {
          m = requireMaybe(path.join("..", defaultDir, fn));
        }
        if (!m) {
          throw new Error("did not find function or problem loading function: " + fn);
        }
        if (typeof m !== "function") {
          throw new Error("function module must export a function");
        }
        allFunctions.push(m);
      });
      return allFunctions;
    }
    function dirExists(dir, cb) {
      var fsExists = fs.exists || path.exists;
      fsExists(dir, function(exists) {
        if (!exists) {
          return cb(false);
        }
        fs.stat(dir, function(err, stat) {
          if (err) {
            return cb(false);
          }
          return cb(stat.isDirectory());
        });
      });
    }
    function findFile(fileList, ifCb, elseCb) {
      var fsExists = fs.exists || path.exists;
      async.detect(fileList, fsExists, function(file) {
        if (file) {
          ifCb(file);
        } else {
          elseCb();
        }
      });
    }
    function findDir(dirList, ifCb, elseCb) {
      async.detect(dirList, dirExists, function(dir) {
        if (dir) {
          ifCb(dir);
        } else {
          elseCb();
        }
      });
    }
    function localizePaths(pathList) {
      pathList = pathList.map(function(p) {
        var resolvedPath = path.resolve(p);
        if (resolvedPath.substr(resolvedPath.length - 3) === ".js") {
          resolvedPath = resolvedPath.substr(0, resolvedPath.length - 3);
        }
        return resolvedPath;
      });
      var localizedPathList = [];
      if (pathList.length > 0) {
        var commonPath = null;
        pathList.forEach(function(fullPath, i) {
          var parts;
          if (commonPath === null) {
            parts = fullPath.split("/");
            if (parts.length > 1) {
              commonPath = parts.slice(0, parts.length - 1).join("/") + "/";
            } else {
              commonPath = "";
            }
          } else if (commonPath.length > 0) {
            parts = commonPath.split("/");
            parts.pop();
            commonPath = "";
            for (var partIndex = 0; partIndex < parts.length; partIndex++) {
              var testPath = commonPath + parts[partIndex] + "/";
              if (fullPath.substring(0, testPath.length) === testPath) {
                commonPath = testPath;
              } else {
                break;
              }
            }
          }
        });
        pathList.forEach(function(fullPath, i) {
          localizedPathList.push(fullPath.substr(commonPath.length));
        });
      }
      return localizedPathList;
    }
    function cleanUndefinedProperties(obj) {
      var newObj;
      var isArray = false;
      if (typeof obj === "object" && obj !== null) {
        if (Array.isArray(obj)) {
          newObj = [];
          isArray = true;
        } else {
          newObj = {};
        }
      } else {
        return obj;
      }
      var keys2 = Object.keys(obj);
      keys2.forEach(function(key, i) {
        if (isArray || typeof obj[key] !== "undefined") {
          newObj[key] = cleanUndefinedProperties(obj[key]);
        }
      });
      return newObj;
    }
    function isCapitalized(string) {
      return string && string.match(/^[A-Z]/) ? true : false;
    }
    module2.exports.findRules = findRules;
    module2.exports.findFunctions = findFunctions;
    module2.exports.findFile = findFile;
    module2.exports.findDir = findDir;
    module2.exports.toArray = toArray;
    module2.exports.localizePaths = localizePaths;
    module2.exports.cleanUndefinedProperties = cleanUndefinedProperties;
    module2.exports.isCapitalized = isCapitalized;
  }
});

// ../../../../node_modules/doctor/lib/nice-ast.js
var require_nice_ast = __commonJS({
  "../../../../node_modules/doctor/lib/nice-ast.js"(exports2) {
    var fs = require_fs();
    var path = require_path();
    var fs = require_fs();
    var async = require_async();
    var fsCompare = require_fs_compare();
    var quickpeg = require_quickpeg().config({ pegjs: { cache: true } });
    var AstNode = require_ast_node();
    var u = require_util2();
    function walk(node, beforeCb, afterCb) {
      if (node._removed) {
        return;
      }
      if (node.type) {
        beforeCb(node);
      }
      if (node.nodes) {
        var copyNodes = node.nodes.slice(0);
        copyNodes.forEach(function(childNode) {
          walk(childNode, beforeCb, afterCb);
        });
      }
      if (node.type) {
        if (typeof afterCb === "function") {
          afterCb(node);
        }
      }
    }
    function walkEnd(node, cb) {
      walk(
        node,
        function(node2) {
          cb(node2);
        },
        function(node2) {
          if (node2.nodes) {
            cb({ type: "end", node: node2 });
          }
        }
      );
    }
    function extendAst(node, parent) {
      node = new AstNode(node);
      node.parent = parent || null;
      node.prev = null;
      node.next = null;
      if (node.nodes) {
        var children = node.nodes;
        node.nodes = [];
        children.forEach(function(childNode, i) {
          if (childNode.type) {
            node.nodes.push(extendAst(childNode, node));
          } else {
            node.nodes.push(childNode);
          }
        });
        node.nodes.forEach(function(childNode, i) {
          if (childNode.type) {
            if (i > 0) {
              childNode.prev = node.nodes[i - 1];
            }
            if (i < node.nodes.length) {
              childNode.next = node.nodes[i + 1];
            }
          }
        });
      }
      return node;
    }
    function cleanAst(node) {
      var copy = {};
      Object.keys(node).forEach(function(key, i) {
        if (key !== "parent" && key !== "prev" && key !== "next" && key !== "_items") {
          copy[key] = node[key];
        }
      });
      node = copy;
      if (node.nodes) {
        var children = node.nodes;
        node.nodes = [];
        children.forEach(function(childNode, i) {
          if (childNode.type) {
            node.nodes.push(cleanAst(childNode));
          } else {
            node.nodes.push(childNode);
          }
        });
      }
      return node;
    }
    function pegParser(options2, extension, cb) {
      if (typeof cb === "undefined") {
        cb = extension;
        extension = null;
      }
      var grammarFiles = [];
      if (typeof options2 === "string") {
        options2 = { grammar: options2 };
      }
      var grammar = options2.grammar;
      if (typeof grammar === "object") {
        if (grammar[extension]) {
          grammar = grammar[extension];
        }
      }
      if (typeof grammar !== "string") {
        if (extension === "js") {
          grammar = "javascript";
        } else if (extension === "md") {
          grammar = "markdown";
        }
      }
      if (grammar) {
        grammarFiles.push(grammar);
        grammarFiles.push(path.join(__dirname, "../grammar", grammar + ".pegjs"));
      } else {
        grammarFiles.push(path.join(__dirname, "../grammar/javascript.pegjs"));
      }
      function findGrammarFile(wrapper, cb2) {
        var grammarFile = wrapper.file;
        var cachedParser = quickpeg.parserFromMemory(grammarFile);
        if (cachedParser) {
          wrapper.parser = cachedParser;
          return cb2(true);
        }
        var fsExists = fs.exists || path.exists;
        fsExists(grammarFile, function(exists) {
          if (!exists) {
            return cb2(false);
          }
          quickpeg(grammarFile, function(err, cachedParser2) {
            if (err) {
              wrapper.err = err;
              return;
            }
            wrapper.parser = cachedParser2;
            return cb2(true);
          });
        });
      }
      var grammarFileWrappers = grammarFiles.map(function(file) {
        return {
          parser: null,
          file,
          err: null
        };
      });
      async.detectSeries(grammarFileWrappers, findGrammarFile, function(wrapper) {
        if (wrapper.err || wrapper.parser) {
          return cb(wrapper.err, wrapper.parser, wrapper.file);
        }
        cb(new Error("grammar file not found"));
      });
    }
    function addLineNumbers(ast2, source2) {
      var i;
      var sourcePosition = 0;
      var sourceLine = 1;
      var sourceColumn = 1;
      var prevBreak = false;
      walk(ast2, function(node) {
        if (typeof node.pos !== "undefined") {
          if (node.pos < sourcePosition) {
            sourcePosition = 0;
            sourceLine = 1;
            sourceColumn = 1;
          }
          for (i = sourcePosition; i < node.pos; i++) {
            var c = source2.charAt(i);
            if (c === "\n") {
              if (!prevBreak) {
                sourceLine++;
              }
              sourceColumn = 1;
              prevBreak = false;
            } else if (c === "\r" || c === "\u2028" || c === "\u2029") {
              sourceLine++;
              sourceColumn = 1;
              prevBreak = true;
            } else {
              sourceColumn++;
              prevBreak = false;
            }
          }
          node.line = sourceLine;
          node.column = sourceColumn;
          sourcePosition = i;
        }
      });
    }
    function astFromSource(options2, source2, extension, cb) {
      if (typeof cb === "undefined") {
        cb = extension;
        extension = null;
      }
      pegParser(options2, extension, function(err, parser, key) {
        if (err) {
          return cb(err);
        }
        var ast2;
        try {
          ast2 = parser.parse(source2);
          addLineNumbers(ast2, source2);
          if (ast2.type === "program") {
            ast2.type = "script";
          } else if (ast2.type === "markdown") {
            ast2.content = source2;
          }
          ast2.grammarFile = key;
          walk(ast2, function(node) {
            if (!node.comments && node.type !== "undefined") {
              node.comments = [];
            }
          });
        } catch (e) {
          return cb(e);
        }
        return cb(null, ast2);
      });
    }
    function astFromFile(options2, file, cb) {
      function convertSourceFile(file2, packageName, packagePath) {
        fs.readFile(file2, "utf-8", function(err, source2) {
          if (err) {
            return cb(err);
          }
          var extension = path.extname(file2);
          if (extension[0] === ".") {
            extension = extension.substr(1);
          }
          astFromSource(options2, source2, extension, function(err2, ast2) {
            if (err2) {
              err2.file = file2;
              return cb(err2);
            }
            if (packageName) {
              ast2.package = { name: packageName, path: packagePath };
              if (packagePath[0] !== "/") {
                ast2.package.path = path.join(process.cwd(), packagePath);
              }
              ast2.package.originalPath = packagePath;
            }
            ast2.originalPath = file2;
            if (file2[0] === "/") {
              ast2.path = file2;
            } else {
              ast2.path = path.join(process.cwd(), file2);
            }
            return cb(null, ast2);
          });
        });
      }
      function convertPackage() {
        if (path.basename(file) === "package.json") {
          fs.readFile(file, "utf-8", function(err, pkgString) {
            try {
              var pkg = JSON.parse(pkgString);
              if (!pkg.main) {
                return cb(new Error(file + " has no main modules."));
              }
              var main = path.join(path.dirname(file), pkg.main);
              if (!main.match(/\.js$/)) {
                main = main + ".js";
              }
              convertSourceFile(main, pkg.name, file);
            } catch (packageParseError) {
              packageParseError.message = "Error parsing " + file + " - " + packageParseError.message;
              return cb(packageParseError);
            }
          });
        } else {
          convertSourceFile(file, null);
        }
      }
      convertPackage();
    }
    function prepareRules(options2, rules, defaultDir) {
      rules = u.findRules(options2, rules, defaultDir);
      var anyStartRules = [];
      var anyEndRules = [];
      var typeToRules = { "": anyStartRules, "end": anyEndRules };
      rules.forEach(function(rule) {
        var ruleTypes = rule.type || [];
        ruleTypes = Array.isArray(ruleTypes) ? ruleTypes : [ruleTypes];
        if (ruleTypes.length === 0) {
          ruleTypes.push("");
        }
        ruleTypes.forEach(function(ruleType) {
          if (ruleType === "") {
            anyStartRules.push(rule);
            Object.keys(typeToRules).forEach(function(key) {
              if (key !== "" && key !== "end" && key.substring(0, 4) !== "end-") {
                typeToRules[key].push(rule);
              }
            });
          } else if (ruleType === "end") {
            anyEndRules.push(rule);
            Object.keys(typeToRules).forEach(function(key) {
              if (key !== "" && key.substring(0, 4) === "end-") {
                typeToRules[key].push(rule);
              }
            });
          } else {
            if (!(ruleType in typeToRules)) {
              typeToRules[ruleType] = [];
              if (ruleType.substring(0, 4) !== "end-") {
                typeToRules[ruleType] = [].concat(anyStartRules);
              } else {
                typeToRules[ruleType] = [].concat(anyEndRules);
              }
            }
            typeToRules[ruleType].push(rule);
          }
        });
      });
      return typeToRules;
    }
    function walkWithRules(options2, rules, defaultDir, ast2, nodeCb, finalCb) {
      var typeToRules;
      try {
        typeToRules = prepareRules(options2, rules, defaultDir);
      } catch (e) {
        return finalCb(e);
      }
      options2._walkQueue = [];
      function eachNode(node, queue) {
        var nodeType = node.type;
        var isEndNode = false;
        if (nodeType === "end") {
          isEndNode = true;
          nodeType = nodeType + "-" + node.node.type;
          node = node.node;
        }
        var ruleType = nodeType in typeToRules ? nodeType : "";
        if (ruleType === "" && isEndNode) {
          ruleType = "end";
        }
        var rules2 = typeToRules[ruleType];
        for (var i = 0; i < rules2.length; i++) {
          var rule = rules2[i];
          if (typeof rule.match === "function") {
            if (!rule.match(node)) {
              continue;
            }
          }
          nodeCb(node, rule);
        }
        if (!queue) {
          while (options2._walkQueue.length > 0) {
            var nextNode = options2._walkQueue.splice(0, 1)[0];
            eachNode(nextNode, true);
          }
        }
      }
      try {
        walkEnd(ast2, eachNode);
        delete options2._walkQueue;
      } catch (e) {
        return finalCb(e);
      }
      finalCb(null, ast2);
    }
    function lispify(node) {
      if (!("value" in node) && !node.nodes) {
        return "(" + node.type + ")";
      }
      if ("value" in node) {
        return "(" + node.type + " " + JSON.stringify(node.value) + ")";
      }
      var buffer = ["(" + node.type];
      node.nodes.forEach(function(childNode, i) {
        buffer.push(" " + lispify(childNode));
      });
      buffer.push(")");
      return buffer.join("");
    }
    function isValueNode(node) {
      if (!("nodes" in node)) {
        return true;
      }
      return false;
    }
    function like(a, b) {
      if (a.type === "var" && b.type === "vars") {
        if (b.nodes.length === 1) {
          b = b.nodes[0];
        }
      }
      if (b.type === "name" && (b.value === "__" + a.type + "__" || b.value === "__any__")) {
        return true;
      }
      if (a.type !== b.type) {
        return false;
      }
      if (a.value !== b.value) {
        return false;
      }
      if (isValueNode(a) !== isValueNode(b)) {
        return false;
      }
      if (!isValueNode(a)) {
        if (a.nodes.length < b.nodes.length) {
          return false;
        }
        for (var i = 0; i < b.nodes.length; i++) {
          if (!like(a.nodes[i], b.nodes[i])) {
            return false;
          }
        }
      }
      return true;
    }
    function equalDiff(a, b, diff) {
      diff.a = {
        line: a.line,
        column: a.column
      };
      diff.b = {
        line: b.line,
        column: b.column
      };
      return false;
    }
    function equal(a, b, diff) {
      diff = diff || {};
      if (a.type !== b.type) {
        return equalDiff(a, b, diff);
      }
      if (a.value !== b.value) {
        return equalDiff(a, b, diff);
      }
      if (isValueNode(a) !== isValueNode(b)) {
        return equalDiff(a, b, diff);
      }
      if (!isValueNode(a)) {
        if (a.nodes.length !== b.nodes.length) {
          return equalDiff(a, b, diff);
        }
        for (var i = 0; i < b.nodes.length; i++) {
          if (!equal(a.nodes[i], b.nodes[i], diff)) {
            return false;
          }
        }
      }
      return true;
    }
    function parserForGrammar(grammarFile) {
      return quickpeg.parserFromMemory(grammarFile);
    }
    exports2.walk = walk;
    exports2.walkEnd = walkEnd;
    exports2.extendAst = extendAst;
    exports2.cleanAst = cleanAst;
    exports2.astFromFile = astFromFile;
    exports2.astFromSource = astFromSource;
    exports2.walkWithRules = walkWithRules;
    exports2.pegParser = pegParser;
    exports2.lispify = lispify;
    exports2.like = like;
    exports2.equal = equal;
    exports2.parserForGrammar = parserForGrammar;
  }
});

// ../../../../node_modules/doctor/lib/report.js
var require_report = __commonJS({
  "../../../../node_modules/doctor/lib/report.js"(exports2, module2) {
    var nast = require_nice_ast();
    var u = require_util2();
    var _ = require_underscore();
    function Report(options2, ast2) {
      this.options = options2;
      this.ast = ast2;
      this.items = { root: { type: "group", items: [] } };
      this._home = null;
    }
    Report.prototype.run = function(cb) {
      var self = this;
      var currentPath = "unknown";
      function eachNode(node, rule) {
        if (node.path) {
          currentPath = node.path;
        }
        if (typeof rule.report === "function") {
          var reportItems;
          try {
            reportItems = rule.report(node, self);
          } catch (e) {
            e.message = "Problem creating report for " + currentPath + " at line " + node.line + ", column " + node.column + ".\n" + e.message;
            throw e;
          }
          if (reportItems) {
            reportItems = Array.isArray(reportItems) ? reportItems : [reportItems];
            for (var i = 0; i < reportItems.length; i++) {
              var reportItem = reportItems[i];
              self.add(reportItem);
            }
          }
        }
      }
      function setHomePath(report, currentKey, allKeys) {
        allKeys = allKeys || [];
        if (allKeys.indexOf(currentKey) >= 0) {
          return;
        }
        allKeys.push(currentKey);
        if (!report.items.hasOwnProperty(currentKey)) {
          return;
        }
        var item = report.items[currentKey];
        item.isHomePath = true;
        if (currentKey === "root") {
          return;
        }
        var groups = item.groups;
        if (!groups) {
          return;
        }
        _(groups).each(function(group) {
          setHomePath(report, group, allKeys);
        });
      }
      function finishRules(err) {
        if (err) {
          return cb(err);
        }
        var finalReport = {};
        finalReport.items = self.items;
        if (self._home) {
          finalReport.home = self._home;
          setHomePath(finalReport, finalReport.home);
        }
        cb(null, finalReport);
      }
      var rules = u.toArray(self.options.report || "default");
      nast.walkWithRules(self.options, rules, "report", self.ast, eachNode, finishRules);
    };
    Report.prototype.item = function(key, item) {
      if (typeof item === "undefined") {
        return this.items[key];
      }
      if (item.key !== key) {
        item = _.extend({}, item);
        _.extend(item, { key });
      }
      this.add(item);
      return this.items[key];
    };
    Report.prototype.remove = function(key) {
      var self = this;
      if (!(key in self.items)) {
        throw new Error("no item with key " + key + " to remove");
      }
      var item = self.items[key];
      if (item.type === "group" && item.items && item.items.length > 0) {
        throw new Error("cannot delete non-empty group: " + item.key);
      }
      if (item.groups) {
        item.groups.forEach(function(group, i) {
          var groupItem = self.items[group];
          if (groupItem) {
            var index = groupItem.items.indexOf(key);
            if (index >= 0) {
              groupItem.items.splice(index, 1);
              if (groupItem.itemTypeCounts && groupItem.itemTypeCounts[item.type]) {
                groupItem.itemTypeCounts[item.type]--;
              }
            }
          }
        });
      }
      delete self.items[key];
    };
    Report.prototype.add = function(item) {
      var self = this;
      if (!item.key) {
        throw new Error("undefined report item key for:\n" + JSON.stringify(item, null, 2));
      }
      if (self.items[item.key]) {
        self.remove(item.key);
      }
      self.items[item.key] = item;
      if (typeof item.groups === "string") {
        item.groups = [item.groups];
      }
      var newGroups = item.groups || [];
      item.type = item.type || "item";
      item.name = item.name || item.key;
      if (item.type === "group") {
        if (newGroups.length === 0) {
          newGroups.push("root");
          if (!item.groups) {
            item.groups = [];
          }
          item.groups.push("root");
        }
      }
      _(newGroups).each(function(group) {
        var groupItem = self.items[group];
        if (!groupItem) {
          groupItem = { key: group, type: "group", items: [] };
          self.add(groupItem);
        }
        if (!groupItem.items) {
          groupItem.items = [];
        }
        if (!groupItem.itemTypeCounts) {
          groupItem.itemTypeCounts = {};
        }
        if (!groupItem.itemTypeCounts[item.type]) {
          groupItem.itemTypeCounts[item.type] = 0;
        }
        groupItem.items.push(item.key);
        groupItem.itemTypeCounts[item.type]++;
      });
      if (item.isHome) {
        self.home(item.key);
      }
    };
    Report.prototype.home = function(item) {
      if (typeof item === "undefined") {
        return this._home;
      }
      var key = item;
      if (typeof item === "object") {
        key = item.key;
      }
      if (this._home) {
        if (this.items.hasOwnProperty(this._home)) {
          delete this.items[this._home].isHome;
        }
        this._home = null;
      }
      if (key && this.items.hasOwnProperty(key)) {
        this.items[key].isHome = true;
        this._home = key;
      }
    };
    function run(options2, ast2, cb) {
      var report = new Report(options2, ast2);
      report.run(cb);
    }
    module2.exports = run;
  }
});
export default require_report();
//# sourceMappingURL=doctor_lib_report.js.map
